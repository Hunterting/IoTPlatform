# 2026-08-03 工作日志

## 安圣二开协议重构 P3/P4/P5 验收状态核查（只读审计，QA 严过关执行）

用户问「Phase3/4/5 是否完成」。产出报告 `docs/phase345-audit-2026-08-03.md`（862 行）。
`git status` 确认仅新增报告，零生产代码改动。

### ⚠️ 先记一条易踩的坑：项目存在两套 Phase 编号

| 编号体系 | 范围 | 状态 |
|---|---|---|
| **旧套**「安圣 MQTT 设备接入 Phase 1–4」（2026-06-27） | 模型/适配器 → 命令下发 → 设备发现 → API 控制器 | ✅ 全部完成，**只有 4 个 Phase** |
| **新套**「二开协议重构 T1–T14 / P1–P5」（`.workbuddy/design/ansheng-open-redesign.md`） | P1:T1-4 / P2:T5-7 / P3:T8-9 / P4:T10-11 / P5:T12-14 | 见下 |
| 联调内部编号 T01/T02（7-31 后） | 真机联调修复 | 与上述两套均无关 |

问到「Phase 5」必定指新套。
`docs/phase2-smoke-test.ps1.md` 文件名有强误导性（实为旧套联调冒烟脚本），建议改名。

### 核查结论

- **P2（T5–T7）、P3（T8–T9）、P4（T10–T11）全部未开工**；P5 仅 T14 完成约 50%（且系 T2 副产品，非按设计执行）
- 这些任务在代码里**只存在于 T1 的 Catalog 声明层 + T2 的 Builder 构造层**，无一具备完整业务链路
- 待办量 ≈ 37.5 人日 / 37 新增文件 / 30 修改 / 7 Migration

**三级判定口径（本次核查的核心方法，可复用）**：
1. Catalog 有定义（T1 已录 36 method）→ 不等于实现
2. Builder 能构造报文（T2 产物）→ 不等于链路完整
3. Service + Controller + 上行 Handler + 前端 + 测试 → 才算交付

典型：T8 的 `BuildAction`/`BuildActions`/延时三方法在 `AnShengCommandBuilder.cs:324-410` 齐全，
但 Services/Controllers/DTOs/Models 中 `startDelayTask` 等**全文检索零命中**（仅注释）。

### 日志一致性核对：未发现虚报

P1 完成、伪命令后端已删、setMqtt 未实现 —— 代码实证与日志记载全部一致。

### 🔴 最高优先级发现：T4 前端欠账已造成线上坏功能

7-31 约定「T4 延后，待 T8 就绪同批切换，避免空窗期」——**该约定已失效**：后端 T3 已物理删除，T8 至今未开工，空窗期实际发生且还有 ≥12 人日前置依赖。

- `AnShengManagementPage.tsx:119-161` 仍有 4 个伪命令按钮
- `anshengApi.ts:72-100` 三个 API 指向已删端点 → 404（用户决策不保留 410）
- **`getSwitchConfig` 更严重**：未被 if-else 捕获 → 落 `:442` 通用分支 → Catalog `TryGet` 返回 false
  → **`AnShengCommandService.cs:104` 整个校验块被跳过**
  → 适配器 `AnShengMqttProtocolAdapter.cs:355` Legacy 兜底 → **真实下发协议外报文到现网设备**，前端还显示成功

**架构性根因**：「不在 Catalog 中 ⇒ 按 Legacy 下发」是兜底放行，任何拼写错误都会被真实下发而非快速失败。

**建议立即做（0.3 人日，无依赖）**：摘 4 模板 + 3 死 API + 3 个 if-else 分支，并给 Legacy 兜底加白名单。

### 其他关键发现

- **T5 缺位的连带缺陷**：`AnShengCommandSpec.cs:54` 品类校验 `Unknown → return true`（fail-open），
  而 `GetDeviceKind` 靠 `AnShengMqttProtocolAdapter.cs` 静态内存字典、进程重启即丢
  → **重启后所有设备品类校验全部放行**
- **测试覆盖边界**：402/402 数字属实，但 `tests/IoTPlatform.AnSheng.Tests/`（2 文件 2230 行）
  **100% 集中于协议层**，对 Service/Controller/DbContext 零覆盖，无集成测试脚手架
  → T5 之前需先补 `WebApplicationFactory` 脚手架（2 人日），否则 T5–T14 的集成型验收标准无法自动化验证
- **P3 的真实卡点**：7-31「依联调结果决定 Phase 2」的决策点至今未关闭 —— 联调已于 8-03 打通全链路，但未见据此启动 P2

### 建议执行顺序

T4 止血(0.3) ‖ 协议歧义 R3/R8/R2/R4/R11 确认 → 测试脚手架(2) → T5(3)→T6(5)→T7(4) → T8(3)→T9(3) → T10(4)‖T11(5) → T14(1.5)→T12(5)→T13(2)

T9 完成是**用户价值拐点** —— 在此之前二开开关设备在平台上实际不可控。

## T4 止血第 0 步执行完成（团队 software-ansheng-t4-stopbleed：工程师寇豆码 + QA 严过关）

- **改动 4 文件**：前端 `Web/src/app/pages/AnShengManagementPage.tsx`（删 4 伪命令模板 `setSwitch`/`getSwitchStatus`/`setSwitchConfig`/`getSwitchConfig` + 3 个 if-else 分支，5→2 分支，保留 `reboot`/`getDevInfo`/`getDevStatus`）；`Web/src/app/services/api/anshengApi.ts`（删 3 死 API + 2 import type）；`Web/src/app/services/api/types/ansheng.types.ts`（删 3 死类型）；后端 `Infrastructure/Protocol/Adapters/AnShengMqttProtocolAdapter.cs`（Legacy 兜底改「默认拒绝、显式放行」三分支，白名单 `orderStart`/`orderEnd`/`orderUp`，`throw NotSupportedException` 在 `PublishAsync` 之前 21 行）。
- **编译**：前后端均 0 错误（后端 81 既存警告，T4 引入 0 新警告）。
- **QA 独立验证 420/420 通过**（既有 402 + 新增 18 白名单单测，含变异测试证伪：回退适配器后 13/18 失败）。Smart Routing → NoOne（可合入）。新增 `tests/IoTPlatform.AnSheng.Tests/AnShengLegacyWhitelistTests.cs`。
- **未提交**：按 QA 建议已 `git restore Web/dist/index.html`（build 副作用，不纳入提交）。`.gitignore` 大小写修正（`/web/dist`→`/Web/dist/`，当前靠 `core.ignorecase`）+ `git rm --cached Web/dist/index.html Web/dist/assets/cad/README.md` 待独立清理提交，**需用户决策**。
- **后续动作（非本次范围，已建议立项）**：
  - A【高】前端补 `typescript`+`tsconfig.json`+`npm run typecheck` 并入 CI（R1 类型检查缺口，当前工程配置下类型检查物理上无法执行）
  - B【高】修 `orderStart`/`orderEnd`/`orderUp` 前后端参数契约（R3 仍外发无效报文，走 `BuildLegacyCommand` 原样透传缺 `sn`/`order`）→ 归 **T5**
  - C【中】二开 tab 接入 `action`/`actions` 通断 UI（后端已就绪，纯前端缺口）→ 归 **T8**
  - D【低】dist 清理提交 + `.gitignore` 大小写修正
- **线上提醒**：上线前扫生产库定时任务/命令模板表，确认无 4 伪命令残留（会开始返回失败，但降级优雅不 500）。

## 第 1 步：关闭协议歧义（2026-08-03，团队 software-ansheng-close-ambiguity）

- 用户（安圣侧）确认三项协议歧义，设计文档 `ansheng-open-redesign.md` 已固化（架构师高见远，7 处 Edit）：
  - **R3 关闭**：`setSlotTimeTasks` 含 `slotNum`（必填 int 1 起），`getSlotTimeTasks.slotNum` 可选 → T10 单插槽版解禁，取消原整表版 fallback
  - **R8 关闭**：`getEMStatistics.data[]` 按插槽 1~n 顺序（SlotNum=index+1），仍用 Profile.SlotAmount 校验长度
  - **R2 关闭**：型号↔品类经 `getDevStatus.netType` 推断（值域 {4G,WiFi}），不再等型号↔品类对照表
  - **R4/R11 仍待 T12**：simCheck 字段、beta 命令稳定性，用户未答复
- 审计报告 `docs/phase345-audit-2026-08-03.md` 已由主理人同步 8 处过期 R 项状态（T5/T10/T11 行 + 第1步清单 + 第5步 fallback 删除）
- **架构师建议待办**：R9（`getTimeTasks.tasks[]` 同 R8 按插槽顺序）与 R8 同构，用户仅字面确认 getEMStatistics 未明确 getTimeTasks；建议下次问安圣补确认（暂保持开启）
- 设计文档收尾已完成（架构师）：L303 引用修正 + T10 验收补 `slotNum` 越界校验条款（第7条）
- **关键认知**：设计文档有两套独立 R 编号——§6（命令可靠性 R1-R9）与 §7（协议歧义 R2/R8/R11 等），互不对应；§7 已加消歧说明
- **影响评估**：R2/R3/R8 关闭不改变 P2 启动顺序（T5→T6→T7），但 T5 品类判定改走 netType、T10 需实现单插槽版、T11 按 index+1 映射；P4(T10/T11) 前置依赖未变

## 第 2 步：关 R9 + 测试脚手架（2026-08-03，团队 software-ansheng-step2-testscaffold）

用户拍板「关 R9 + 启动第 2 步测试脚手架」。

### R9 关闭（架构师高见远，同构推定）
- `ansheng-open-redesign.md` L2060（R9 行）+ L2049（关闭进度注）：`getTimeTasks.tasks[]` 按插槽 1~n 顺序、SlotNum=index+1，同构推定关闭（与已关 R8 同构，非厂商逐条确认），继承 Profile.SlotAmount 长度校验护栏。审计报告 L776 补录 R9 关闭行。
- **现状**：§7 协议歧义项 R2/R3/R8/R9 已关闭，R4/R11 仍待 T12。

### 测试脚手架实现（工程师寇豆码 + QA 严过关）
- **方案文档**：`.workbuddy/design/ansheng-test-scaffold-design.md`（729 行）+ 3 图（class/sequence/tasks mermaid）。
- **关键选型（实读代码，非推测）**：
  - DB：本机无 Docker（Testcontainers 跑不了）→ 用 **真实 MySQL 独立 schema**（IDbProvisioner 抽象，未来可切 Testcontainers）；**InMemory 明确否决**（启动即 Migrate、HasColumnType("json")、RowVersion 并发、迁移本身是验收对象）
  - 用户决策：测试库 = `192.168.3.7:3306`（团队共享开发库，非本机），凭据走环境变量 `IOT_TEST_MYSQL`，独立 schema 跑完 DROP
  - MQTT 真实接缝 = `IProtocolAdapterFactory` → `RecordingAnShengAdapter`（IMqttClient 不在 DI）
  - 认证 = `TestAuthHandler`（X-Test-Role/AppCode 头驱动 claim）
  - 清理 = Respawn 6.2.1 + 禁用并行（静态状态所迫）
- **交付**：`tests/IoTPlatform.IntegrationTests/` 21 文件（net8.0 + FrameworkReference AspNetCore.App）；`Program.cs` 追加 1 行 `public partial class Program {}`（**唯一生产改动**）；`IoTPlatform.sln` 注册两个测试工程。
- **结果**：对 192.168.3.7 真实 MySQL **10/10 全绿**，复跑 8 次稳定，schema 建后即删零残留。
- **QA 独立验证 434/434 全绿**（420 既有协议单测 + 14 集成：10 示例 + 4 QA 证伪），双变异测试证伪发现工程师 2 处自测盲区（替身分身清理原零覆盖→QA-02 锁死；示例-04 为假保障→QA-01 修正），1 项文档返工（TestAuthHandler.cs 注释口径，已改）、**生产改动范围审计零越界**。
- **⚠️ 实测环境事实**：MySQL 实际 = **5.7.26**（非 8.x）；实例含 48 库 + root 账号（R3 共享库并发风险，建议 CI 换独立实例或建专用 DDL 账号）。

### T5–T14 开工前置约束（已钉进测试/文档）
1. 权限用例：匿名断 `StatusCode==401`（裸空包体），越权断 `Code==403`，**勿对匿名响应调 ReadAsync**
2. 「协议外报文被拒」：断 `ErrorMessage` 含"不属于安圣二开协议目录" **且** `Adapter.Sent` 为空（比示例-06 更严）
3. 多租户真隔离必须**另起 TestWebAppFactory**（单 Factory 固定 AppCode=TEST，因 EF 模型缓存冻结 AppCode）
4. 目标库 **MySQL 5.7.26**，勿用 8.0 专有语法
5. 顺带发现的待立项生产缺陷：**租户全局过滤器被 EF 模型缓存冻结**（`AppDbContext.cs:154-189` 在 OnModelCreating 里把 AppCode 烘焙进模型，全仓无 `IModelCacheKeyFactory` 替换 → 谁先触发建模谁的 AppCode 冻结到容器生命周期，多租户先到先得正确性问题）；建议在 T5 前单独立项修复，不在本次范围

（注：日志在 "IM" 处被截断，但第 2 步的核心交付、选型、验证结果、T5–T14 约束已全部记录；后续若需补全该生产缺陷的完整描述可再追加。）

## 启动 P2·T5（设备能力档案 Profile 与品类判定）— 2026-08-03 晚

工程实现 + 独立验证**全部完成，IS_PASS: YES**（团队 software-ansheng-t5-profile：架构师高见远出设计、工程师寇豆码实现、QA严过关独立验证）。

### 最终验证结果（严过关第 2 轮，真跑非采信）
- 编译 0 error/0 warning；单测 **564 passed**（560→564，净+4 新护栏/覆盖）；集成 **16 passed / 0 skipped**（含 F24 + QA 补的 #4 超时用例）
- 四条验收全过：#1 迁移可逆（Up7列↔Down7列对称，int枚举无ENUM/CHECK）；#2 认领后Profile非空+Category=Kind.ToDisplayName()（生产码"安圣充电桩"0命中，已证伪）；#3 InferKind三级（WiFi/4G→Speaker，Manual短路）；#4 探测失败显式报错（Code=400+ErrorCode=PROBE_FAILED，已证伪）
- **4G 无线索语义裁定（主理人+架构师共识）**：`("4G",null,null)→Speaker4G`，与 WiFi 对称；理由「无插槽证据即非开关⇒喇叭」。设计文档 D3 L302 原只写 WiFi 行系欠规范，架构师已回填 §3.4bis。

### ⚠️ 关键过程价值：560 全绿是假绿
QA 首轮照出 `InferKind("WiFi",null,null)` 仍返回 Unknown（即我最初标记的差异）——根因 `AnShengDeviceKind.cs` 三级分支多写的 `Unknown` 早返回 + 两条把错误行为焊成契约的回归测试（InlineData("WiFi")→Unknown / "无slotAmount即≡Resolve"）。工程师撤回上轮过早 IS_PASS，删早返回、重写测试、补 null 用例、加永久护栏 `InferKind_Must_Be_Strictly_Stronger_Than_Resolve_On_N3_Scenario`（改回退化立刻红）。**教训：照实现写的测试套件规模再大也不构成验收证据**。

### 🔴 端到端照出的真实生产缺陷（T1/T2 存量，T5 首曝）
`AnShengDiscoveryService.ClaimAsync` 直接 `BeginTransactionAsync`，与 `Program.cs:61` `EnableRetryOnFailure`（MySqlRetryingExecutionStrategy）冲突 → **线上每次认领 100% 进 catch 返回 PersistFailed，认领功能实际不可用**。修复：`CreateExecutionStrategy().ExecuteAsync` 包裹整段事务。另一处 `TestWebAppFactory.RemoveBackgroundServices` 误摘 `IAnShengDiscoveryService` 单例致 DI 500，改为只摘 IHostedService+IMqttClientService。

### 用户两项决策落地
- Q5 不回填存量（迁移只建表不写数，存量 Profile=null，读取全容错）
- Q8 Category=Kind.ToDisplayName() 派生中文名（Switch4G→"4G开关"等），前端按"安圣充电桩"过滤失效需后续同步（不在 T5 范围）

### 交付文件（未 commit，按工程师/QA 约定保留）
- 新增生产：`Models/AnShengDeviceProfile.cs`、`Configuration/AnShengProbeOptions.cs`、`Infrastructure/Protocol/AnSheng/AnShengUplinkHub.cs`、`Services/AnShengDeviceProfileService.cs`(+I)、`Services/AnShengProbeService.cs`(+I)、`Migrations/20260803122119_AddAnShengProfileAndDiscoveredColumns.cs`(+Designer)
- 修改生产：`Controllers/AnShengController.cs`、`DTOs/Requests|Responses/AnSheng*.cs`、`Data/AppDbContext.cs`、`Helpers/ApiResponse.cs`、`Adapters/AnShengMqttProtocolAdapter.cs`、`AnSheng/AnShengDeviceKind.cs`、`AnShengMessageTypes.cs`、`Models/DiscoveredAnShengDevice.cs`、`Program.cs`、`Services/AnShengDiscoveryService.cs`、`Services/Interfaces/IAnShengDiscoveryService.cs`、`Migrations/AppDbContextModelSnapshot.cs`、`appsettings.json`
- 测试：`IoTPlatform.AnSheng.Tests`（6 文件）、`IoTPlatform.IntegrationTests/AnSheng/AnShengClaimTests.cs`、脚手架 `RecordingAnShengAdapter`/`StaticStateResetter`/`IntegrationTestBase`/`TestWebAppFactory` 扩展、`appsettings.Testing.json`(Probe:TimeoutMs)
- 设计：`H:\IoTPlatform\.workbuddy\design\t5-profile-system-design.md`（架构师，1551 行，含 §3.4bis 裁决）

### 遗留/跟进（不阻塞交付）
1. **MSB3277** EFCore.Relational 8.0.2 vs 8.0.11 冲突——存量告警，建议统一对齐包版本
2. **设计文档笔误**：`t5-profile-system-design.md:948-952` 伪代码用 `IMEI_CONFLICT`/`NO_PROTOCOL_CONFIG`，但 §8.3 错误码表无此二码（代码遵循 §8.3 正确）——建议架构师修文档
3. **ClaimAsync 重试潜在双插**（观察项）：`ExecuteAsync` 委托入口建议加 `db.ChangeTracker.Clear()`（真实瞬时故障才触发，难测）；工程师本轮未落此防御，建议 T6/T7 顺带
4. **T6 偏离待对齐**：设计稿 §4.3「档案不存在⇒返回 null 不建档」与当前 `RefreshAsync` 调 `GetOrCreateAsync`（会建档）冲突，T6 接通上行自学习前必须二选一并更新文档（架构师已标注）
5. 前端同步：Category 不再"安圣充电桩"，前端 AnShengManagementPage 按字面过滤需改（归前端，不在 T5）

### 下一步建议
- 提交 T5（建议单独分支/PR，含迁移 + 生产修复 + 测试）；迁移上线前在真实 5.7.26 跑 up→down→up
- 启动 T6（事件识别与处理管道），注意 #4 的 RefreshAsync/GetOrCreateAsync 偏离先决
- 修 MSB3277 包版本、回填设计文档笔误、加 ClaimAsync ChangeTracker.Clear()

用户指令「启动 P2 的 T5」。走增量开发 SOP（跳过 PM：设计文档已是规格）：架构师→工程师→QA。

### 实读代码确认的前置状态（关键，避免踩空）
- **T1/T2 已落地**（与 8-03 报告「P2 未开工」不矛盾：报告指 T5–T7 业务链路，T1/T2 属 P1 协议层早已完成）：
  - `AnShengDeviceKind.cs`：枚举为**顺序值**（Unknown=0,Speaker4G=1,Switch4G=2,SpeakerWiFi=3,SwitchWiFi=4）非 [Flags]；另 `AnShengDeviceCapability`([Flags]) 位掩码 + 扩展方法
  - `AnShengDeviceKindResolver.Resolve(netType, version, model)`：仅按 version/model 字符串判定开关/喇叭 → **缺 slotAmount 参数，无法做设计 D3 二级回退**
  - `AnShengCommandCatalog/Spec/Builder/FirmwareVersion/CommandThrottle` 等均齐
- **T5 缺口**：`AnShengDeviceProfile` 三件套不存在；`DiscoveredAnShengDevice` 缺 Kind/SlotAmount/Version/Iccid/ProbeStatus；`AppDbContext` 无 Profile DbSet；`ClaimDevice` 写死 `Category="安圣充电桩"`、无 Kind、不探测
- **核心差异**：设计验收#3 `InferKind("WiFi", null, null)==SpeakerWiFi`，但 `Resolve("WiFi",null,null)` 当前返回 Unknown → T5 必须补 `InferKind(netType, slotAmount, version)` 三级回退（二级靠 netType+slotAmount）
- 测试脚手架 T5 集成示例 `ProfileAndKindInferenceSampleTests.cs` 尚未创建（§9.1 是占位），QA 阶段按 §9.1 落地

### 团队与进度
- 团队 `software-ansheng-t5-profile`（主理人建）。当前：架构师高见远出增量设计+任务清单（重点：认领强制探测机制 / InferKind 三级归并 / Profile 表+迁移 / 移除写死 Category）。
- 待：工程师寇豆码实现 → QA 严过关（InferKind 单测 + §9.1 集成 + 回归 434 基线）。
- ⚠️ 已知关联待办（非 T5 范围，T5 后跟进）：①租户过滤器冻结缺陷立项；②前端补全 typecheck 入 CI；③orderStart/orderEnd/orderUp 前后端契约修复（归 T5 关联但本任务不强制）；④`AnShengCommandSpec` Unknown→return true fail-open（T5 Profile 落地后可根治）。

## T6 事件管道 — 启动 / 架构设计落盘 (2026-08-03 22:37)
- 团队 software-ansheng-t6-event 已建；架构师高见远交付 t6-event-pipeline-design.md（767 行）+ 类图/时序图 mermaid。
- 命门已核实：适配器 OnMessageReceivedAsync:535 Publish 在 Will 早退(538-542)之前，close 永不达 DataReceived ⇒ 管道必须订阅 AnShengUplinkHub（决策 B-1），不能挂 ProtocolConfigService（否则验收#5 物理不可达）。
- 关键决策：B-1 Hub 订阅(Singleton Pipeline)；B-2 双写默认不抑制；①在途表 T6 前移最小实现/T7 增强；②recv485 不建专用表(走 DataCollection)；③AnShengDeviceEvent 字段+独立 Singleton AnShengOfflineDebouncer(30s 去抖，摘除 DiscoveryService 对 DeviceWill 直连)；④simCheck 软白名单(Router 层，不动 Catalog)。
- 决策 A：RefreshAsync 改回 §4.3(返回 null 不建档)；T5 设计 §605-614 已由架构师标注已解决。
- 任务 T6-1..T6-5（5 个 P0），触达 34 文件。验收 6 条(3 单元+3 集成)。部署假设：单实例(W1)。
- 工程师寇豆码编码进行中；QA 独立验证待工程师交付。
