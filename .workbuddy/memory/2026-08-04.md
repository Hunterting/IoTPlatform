## T6 第 2 轮验证状态核查 (2026-08-04 09:22)
- 用户问「T6 第 2 轮回归验证通过了吗」。结论：单元层 PASS，集成层未确认。
- 独立重跑 `IoTPlatform.AnSheng.Tests`：**616 passed / 0 failed**（含 T6 新增 AnShengEventRoutingTests + AnShengEventPipelineUnitTests，覆盖验收 #1/#2/#3）。仅 MSB3277 警告(EFCore.Relational 8.0.2 vs 8.0.11，非阻断，T5 遗留)。
- 集成测试工程编译成功（已成功生成）；但本沙箱 MySQL 192.168.3.7:3306 不可达，验收 #4/#5/#6 无法在此跑。
- T6 团队 software-ansheng-t6-event 在会话压缩后被自动清理（teams/tasks 目录已不存在），QA 第 2 轮在途结论句柄丢失；需重跑集成或用户提供可达 MySQL 才能钉死 #4/#5/#6。
- 待办：用户确认 MySQL 可达方式后重跑集成；或用户本地跑 `dotnet test tests/IoTPlatform.IntegrationTests --filter "FullyQualifiedName~AnSheng"`。仍建议：单实例部署假设(W1)核对、MSB3277 修复、T7 前移的在途表增强、recv485 不建专用表(W4)、simCheck 双向语义回写 A.3(W5)。

## T6 集成测试缺失 — 路1重跑发现的真问题 (2026-08-04 09:48)
- 路1重跑：MySQL 192.168.3.7:3306 现可达（python socket 实测 REACHABLE）。连接串走根 appsettings DefaultConnection，Database 被框架替换为一次性 schema。
- 集成测试 `dotnet test ... --filter "FullyQualifiedName~AnSheng"` 仅匹配 **2 个**（T5 的 AnShengClaimTests 验收#2/#4），2s 跑完全绿。
- **关键发现**：IntegrationTests/AnSheng/ 下只有 AnShengClaimTests.cs，**T6 的事件集成测试（验收#4/#5/#6 的接线+落库层）从未落盘**。工程师写的 AnShengEventPipelineUnitTests.cs 头部明确声明单测只覆盖数据层，集成层由 IntegrationTests 负责——但该层缺失。
- 结论：T6 **不能在「第2轮全绿」意义上通过**——#4/#5/#6 缺集成层验证（单测数据层已覆盖）。
- 处理：重新建队 software-ansheng-t6-itests，派 QA 严过关补建 AnShengEventTests.cs（3 条 Fact：#4 keyEvent 落库+DataRule、#5 close 窗口内 connected 不置离线、#6 getDevStatus→DeviceDataRecord 含 slot1_*/temperature）并在真实 MySQL 跑通。主理人不代写。
- 待 QA 回传 + 主理人重跑复核。

## T6 最终验证完成 (2026-08-04 ~10:30)
- QA Round 2 全绿：AnShengEventTests 4/4（验收#4/#5/#5护栏/#6），路由 NoOne，0 编译错误。
- 主理人独立复核：集成 `FullyQualifiedName~AnSheng` = 6 通过（T5 认领2 + T6 事件4），0 失败；单测 `IoTPlatform.AnSheng.Tests` = 616 通过，0 失败。
- 根因修复：工程师补 `Migrations/20260804023839_AddAnShengDeviceEvent.cs`(+Designer)，Up 仅 CreateTable("AnShengDeviceEvents")+3 索引，Down 仅 DropTable；`has-pending-model-changes` 零漂移；源码0改动、测试文件0改动。
- T6 完整交付：生产 12新源+11改 + 事件表迁移 + QA 集成测试(3验收+1护栏)。全部未 commit。
- 遗留(非阻断)：MSB3277(EFCore.Relational 8.0.2 vs 8.0.11)；设计待办 W1(单实例部署假设)W2(T7 由7新改5新2改回写)W4(recv485不建专用表)W5(simCheck双向语义回写A.3)；#4 的 DataRule→告警端到端尾链归 T9。
- 团队 software-ansheng-t6-itests 已清理（TeamDelete）。
