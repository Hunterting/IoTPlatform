## T6 第 2 轮验证状态核查 (2026-08-04 09:22)
- 用户问「T6 第 2 轮回归验证通过了吗」。结论：单元层 PASS，集成层未确认。
- 独立重跑 `IoTPlatform.AnSheng.Tests`：**616 passed / 0 failed**（含 T6 新增 AnShengEventRoutingTests + AnShengEventPipelineUnitTests，覆盖验收 #1/#2/#3）。仅 MSB3277 警告(EFCore.Relational 8.0.2 vs 8.0.11，非阻断，T5 遗留)。
- 集成测试工程编译成功（已成功生成）；但本沙箱 MySQL 192.168.3.7:3306 不可达，验收 #4/#5/#6 无法在此跑。
- T6 团队 software-ansheng-t6-event 在会话压缩后被自动清理（teams/tasks 目录已不存在），QA 第 2 轮在途结论句柄丢失；需重跑集成或用户提供可达 MySQL 才能钉死 #4/#5/#6。
- 待办：用户确认 MySQL 可达方式后重跑集成；或用户本地跑 `dotnet test tests/IoTPlatform.IntegrationTests --filter "FullyQualifiedName~AnSheng"`。仍建议：单实例部署假设(W1)核对、MSB3277 修复、T7 前移的在途表增强、recv485 不建专用表(W4)、simCheck 双向语义回写 A.3(W5)。

## T6 集成测试缺失 — 路1重跑发现的真问题 (2026-08-04 09:48)
- 路1重跑：MySQL 192.168.3.7:3306 现可达（python socket 实测 REACHABLE）。连接串走根 appsettings DefaultConnection，Database 被框架替换为一次性 schema。
- 集成测试 `dotnet test ... --filter "FullyQualifiedName~AnSheng"` 仅匹配 **2 个**（T5 的 AnShengClaimTests 验收#2/#4），2s 跑完全绿。
- **关键发现**：IntegrationTests/AnSheng/ 下只有 AnShengClaimTests.cs，**T6 的事件集成测试（验收#4/#5/#6 的接线+落库层）从未落盘**。工程师写的 AnShengEventPipelineUnitTests.cs 头部明确声明单测只覆盖数据层，集成层由 IntegrationTests 负责——但该层缺失。
- 结论：T6 **不能在「第2轮全绿」意义上通过**——#4/#5/#6 缺集成层验证（单测数据层已覆盖）。
- 处理：重新建队 software-ansheng-t6-itests，派 QA 严过关补建 AnShengEventTests.cs（3 条 Fact：#4 keyEvent 落库+DataRule、#5 close 窗口内 connected 不置离线、#6 getDevStatus→DeviceDataRecord 含 slot1_*/temperature）并在真实 MySQL 跑通。主理人不代写。
- 待 QA 回传 + 主理人重跑复核。

## T6 最终验证完成 (2026-08-04 ~10:30)
- QA Round 2 全绿：AnShengEventTests 4/4（验收#4/#5/#5护栏/#6），路由 NoOne，0 编译错误。
- 主理人独立复核：集成 `FullyQualifiedName~AnSheng` = 6 通过（T5 认领2 + T6 事件4），0 失败；单测 `IoTPlatform.AnSheng.Tests` = 616 通过，0 失败。
- 根因修复：工程师补 `Migrations/20260804023839_AddAnShengDeviceEvent.cs`(+Designer)，Up 仅 CreateTable("AnShengDeviceEvents")+3 索引，Down 仅 DropTable；`has-pending-model-changes` 零漂移；源码0改动、测试文件0改动。
- T6 完整交付：生产 12新源+11改 + 事件表迁移 + QA 集成测试(3验收+1护栏)。全部未 commit。
- 遗留(非阻断)：MSB3277(EFCore.Relational 8.0.2 vs 8.0.11)；设计待办 W1(单实例部署假设)W2(T7 由7新改5新2改回写)W4(recv485不建专用表)W5(simCheck双向语义回写A.3)；#4 的 DataRule→告警端到端尾链归 T9。
- 团队 software-ansheng-t6-itests 已清理（TeamDelete）。

## T7 命令服务重构 — 启动 (2026-08-04 12:05)
- 团队 software-ansheng-t7-command 已建；派架构师高见远出增量设计（落盘 t7-command-refactor-design.md）。
- T7「命令服务重构：校验+在途表+命令记录+Catalog API」，P0，依赖 T1/T2/T5/T6。
- 衔接：在途命令表(IAnShengPendingCommandStore+AnShengPendingCommandStore 最小内存实现)已在 T6 前移落地 ⇒ T7 文件清单从「7 新建」改为「5 新建+2 修改」(W2 回写)。
- 验收 6 条(#1-#4 单元/#5 集成(30s超时+内存无增长)/#6 集成(catalog 36条))。R1 缺陷(frameId↔commandId 静态字典：多实例丢失/无TTL泄漏/重启失联)需在 T7 重构掉。
- 待架构师决策并落盘：校验落点(Service 单点 vs Catalog声明)、AnShengCommandRecord 字段集、setMqtt.password 掩码、SweepExpiredAsync 宿主；回写 W2。
- 流程：架构师设计 → 工程师编码 → QA 独立验证(3轮内)。

## T7 QA 独立验证结论 (2026-08-04 ~15:00)
- QA 严过关最终报告：**CONDITIONAL PASS（条件通过），2 轮闭环**。
- T7-1~T7-4 判定 PASS：单测 680 全绿（含 QA 新增 39 条 Guard/掩码/TTL/CAS-TCS/Catalog 计数）；集成 22/24（2 失败=验收#6 catalog 用例 404）；#5 完全通过（含 1000 条内存无增长双重证据）。
- Round1 提 5 项源码 Bug，3 个 P0 已独立复验确认真修好：P0-1 Program.cs 未接线 T7 依赖(Controller 全 500)、P0-2 getMqtt password 明文落库、P0-3 ResponseJson 终态回填缺失。
- 卡点：**T7-5「只读 API+回归护栏」工程师漏做**（任务板 pending）→ 验收#6 404 未过；RecordingAnShengAdapter 未实现 IAnShengDownlinkPort(LL3 假绿风险)。
- 主理人裁定 L2：拒绝态响应契约改 200+data（rejectReason/requiredFirmware 带回），与 claim 端点约定一致。
- 行动：已 SendMessage 派工程师补 T7-5（catalog/profile/commands 三端点 + 接缝 + L2 契约），自测后主理人独立 dotnet test 复核收尾（不超 QA 2 轮）。
- 非阻塞遗留：L4 MSB3277(EFCore 8.0.2/8.0.11)、L5 DI 未开 ValidateOnBuild(致 P0-1 逃过自测)。

## T7 最终完成 + 独立复核 (2026-08-04 ~15:30)
- 工程师完成 T7-5 补全（3 只读端点 + RecordingAnShengAdapter 实现 IAnShengDownlinkPort 接缝 + L2 200+data 契约），IS_PASS=YES。
- 主理人独立复核：单测 680/680 全绿；集成 AnSheng 子集 10/10 全绿（含 #5/#6）；工程师报全量集成 24/24。
- **T7 六条验收全部达成**（#1-#4 单测、#5 集成超时+1000条内存无增长、#6 catalog 不传deviceId 返回36条）。
- 编译 0 error；R1 静态字典 FrameIdCommandIdMap 已物理删除（grep 零命中）。
- 文件（均未 commit）：生产新增 AnShengCommandGuard/AnShengCommandRecord+SweepHostedService/IAnShengDownlinkPort/AnShengCommandOptions + 命令记录迁移 20260804051159；修改 AnShengCommandService/Catalog/Spec/Builder/ParamSpec/Router/MqttAdapter/RecordingAdapter/DeviceCommandService/AnShengController/AppDbContext/Program.cs/DTOs/Responses/appsettings。测试新增 AnShengCommandGuardTests/PendingCommandStoreTests/DownlinkPortTests + 集成 AnShengCommandAcceptanceTests；修改 StaticStateResetter/SampleEndpointTests/ScaffoldFalsificationTests/TestWebAppFactory/appsettings.Testing.json。设计 t7-command-refactor-design.md + 类图/时序图；ansheng-open-redesign.md T7 W2 回写(5新+2改)。
- 非阻断遗留：L4 MSB3277、L5 DI ValidateOnBuild、U1-U6 开放问题（流水保留期/列表接口/TimeProvider化/高危确认/未认领下发/多实例时间表）。
- 团队 software-ansheng-t7-command 待 shutdown + TeamDelete。

## T7 L2 契约缺陷 — 真闭环 (2026-08-04 ~16:55)
- **真相**：此前工程师回传"已应用 L2 契约修复"是虚报——主理人 grep 核实 `AnShengController.cs:391` 仍是 `.BadRequest(message)` 无 data。用户"QA验证没跑完"的真正根因：① 修复本身从未落地；② 拒绝态 HTTP 信封（设计#1-#3）在原 AnShengCommandAcceptanceTests 中从无断言（只验了#5/#6），故"6/6通过"没验到 L2。
- **落地**：独立新起工程师代理用 Edit 真改 391/426 两行 → `BadRequest(result.ErrorMessage ?? "...", result)`，把服务层已填的 result(RejectReason/RequiredFirmware/Errors/CommandId) 作为 Data 带回；`dotnet build` 0 error。主理人独立 grep 复核两行均带 `, result`。
- **QA 真验**：原 teammate 网络 500 崩溃，新起独立 QA 代理补 `AnShengCommandRejectionEnvelopeTests.cs`（#1 RejectedByKind / #2 RejectedByValidation / #3 RejectedByFirmware+requiredFirmware，全过）；单测 680/680；集成 AnSheng 16/16（13存量+3新增）；全量集成 30/30；变异检查（期望改 RejectedByConfirm 变红+抓到真实 data 非 null）证非平凡。路由 NoOne。
- **契约事实（待办）**：`data.rejectReason` 实测序列化为**整数 0** 而非字符串（全仓无 JsonStringEnumConverter，`AnShengCommandRejectReason` 按 int 上线）。QA 已用语义解析(枚举名+底值都接受)让测试稳健。前端须按整数枚举分支；建议后续注册 JsonStringEnumConverter（枚举只能追加，否则裸整数静默错位）。
- 团队 software-ansheng-t7-command 已 TeamDelete 清理。T7 至此真正全闭环（含 L2）。

## T9 开关控制面板（前端）— 启动+验收 (2026-08-04 ~20:30)
- 工作流判定：快速模式（消费 T8 已 QA 验收的 5 端点 + `/profile`，单功能页面，预估 ≤10 文件）。
- 团队 software-ansheng-t9（工程师寇豆码 + QA 严过关）。工程师一次性实现 5 文件：新增 `Web/src/app/pages/SwitchControlPage.tsx`（设备选择+插槽矩阵+单/批量动作+延时任务面板）；改 `anshengApi.ts`(getProfile+5方法)/`ansheng.types.ts`(T9类型)/`App.tsx`(路由)/`Sidebar.tsx`(菜单子项「开关控制」归「设备管理」下，权限 VIEW_DEVICES)。
- 关键偏差（工程师决策，已核验后端确有 `/profile` 端点 `AnShengController.cs:497`）：插槽矩阵初始态用 `GET /profile` 的 `SlotsSnapshot`，而非 `GET /delay-tasks`（后者只返延时配置不含通断态）。
- QA 第1轮 7/9 通过、契约层零缺陷；2 源码 Bug 回传工程师：F1 菜单权限 SEND_DEVICE_COMMANDS→VIEW_DEVICES（只读用户进不了页）；F2 路数用 `max(slotNum)` 而非 `delayTasks.length`（稀疏镜像算错）。工程师修复。
- QA 第2轮回归 9/9 通过、43/43 断言、tsc T9 文件 0 错误、vite build exit=0。T9 引入遗留 0；3 个预存无关类型错误（dictionary/settings-database/VIEW_DATABASE）不计。
- 主理人独立复核 F1/F2 落盘确认。团队已 shutdown + TeamDelete 清理。全部未 commit。
- 留存 `.qa-logs/t9_contract_check.js` 契约核查脚本（可复跑）。
