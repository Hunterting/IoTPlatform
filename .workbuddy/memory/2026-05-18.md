# 2026-05-18 工作日志

## P2 阶段实施完成（ProtocolAdapter 集成 + 时序存储抽象）

### P2-A：ProtocolAdapter 接入主采集链路 ✅
- **改动文件**：`Services/ProtocolConfigService.cs`
- **核心变更**：
  - 新增 `IServiceScopeFactory` 依赖注入
  - 新增 `_activeSubscriptions` 字典追踪事件订阅
  - `StartProtocolAsync()` 中：启动采集后调用 `SubscribeAdapterDataReceived()` 订阅 `adapter.DataReceived` 事件
  - `StopProtocolAsync()` 中：释放适配器前先 `UnsubscribeAdapterDataReceived()` 反注册
  - 新增 3 个私有方法：Subscribe/Unsubscribe/OnProtocolAdapterDataReceived
- **数据流打通**：ModbusTCP/RTU/OPC UA/MQTT(适配器) → DataReceived 事件 → IServiceScopeFactory → DataCollectionService.ProcessDeviceDataAsync() → MySQL
- **关键设计**：异常隔离（单条失败不影响适配器）、Scope 工厂获取 Scoped 服务、字典追踪防泄漏

### P2-B：ITimeSeriesStore 接口 + MySQL 实现 ✅
- **新建文件**：
  - `Services/Interfaces/ITimeSeriesStore.cs` — 时序存储抽象接口（Write/QueryRange/GetLatest/Aggregate/DeleteOlderThan/Statistics）
  - `Services/MySqlTimeSeriesStore.cs` — 基于 DeviceDataRecord 表的 MySQL 实现
- **接口方法**：WriteAsync, WriteBatchAsync, QueryRangeAsync(分页), GetLatestAsync, GetLatestBatchAsync(批量), AggregateAsync(时间窗口聚合), DeleteOlderThanAsync(分批清理), GetStatisticsAsync
- **DI 注册**：Program.cs 中 `AddScoped<ITimeSeriesStore, MySqlTimeSeriesStore>()`
- **扩展预留**：定义了 TimeSeriesAggregateResult / TimeSeriesStatistics 辅助类型

### P2-C：数据保留策略服务（TTL 清理） ✅
- **新建文件**：`Services/DataRetentionHostedService.cs` — BackgroundService 定时清理
- **配置项**：appsettings.json 新增 `DataRetention` 节（Enabled/DetailRetentionDays/CleanupIntervalHours/BatchSize 等）
- **执行模式**：首次延迟 10 分钟 → 每 24 小时循环 → 调用 ITimeSeriesStore.DeleteOlderThanAsync()
- **DI 注册**：Program.cs 中 `AddHostedService<DataRetentionHostedService>()`

### 编译验证
- dotnet build 通过，0 错误，78 个警告均为预先存在的警告

### 架构现状更新
- ~~协议适配器层(Modbus/OPC UA)独立于 MQTT 主链路，DataReceived 事件无消费者~~ ✅ **已修复 (P2-A)**
- ~~时序数据库~~ ✅ **已实现抽象层 ITimeSeriesStore (P2-B/P2-C)，MySQL 实现，预留 InfluxDB 扩展点**
