# 安圣 4G 开关 真机联调报告

- 生成时间(UTC): 2026-07-31 14:00:58
- Broker: `120.79.3.248:18883`
- 连接结果: **已连接**
- 监听模式: 否
- 监听到 IMEI 数: 0
- 下发步骤数: 0（PASS 0 / MISMATCH 0 / TIMEOUT 0 / SKIPPED 0 / ERROR 0）
- 退出码: 2
- 抓包文件: `H:/IoTPlatform/tools/AnShengFieldTest/captures\uplink-20260731-140058.jsonl`

## 一、运行环境

| 项 | 值 |
| --- | --- |
| Host | 120.79.3.248 |
| Port | 18883 |
| Username | admin |
| 上行订阅过滤 (平台 SUBSCRIBE) | `/iot/server/#` |
| 下行发布模板 (平台 PUBLISH) | `/iot/client/iot-board/{imei}` |
| QoS | 1 |
| KeepAlive(s) | 30 |
| 监听时长(s) | 0 |
| --listen-only | False |
| --allow-config | False |
| --allow-control | False |
| 应答超时(s) | 10 |
| 节流(ms) | 100 |
| 目标 IMEI | (监听首个) |
| --kind | (推断) |
| 推断 Kind | - |
| 槽位 | 1 |
| 延时(s) | 10 |

## 二、本地自检（不接触设备）

| 检查 | 结果 | 说明 |
| --- | --- | --- |
| Catalog 命令总数 = 36 | PASS | AnShengCommandCatalog.Count = 36 |
| Catalog 事件方法 = 6 | PASS | EventMethods.Count = 6 |
| setTime 业务 timestamp 不被系统时钟覆盖 | PASS | 下行报文保留业务 timestamp=1700000000 |
| 参数平铺顶层（无 param 包裹） | PASS | 三份样本均无顶层 param 包裹 |
| frameId 16 位且唯一（200 次） | PASS | 长度合规=True, 200 次唯一=True (FrameIdLength=16) |
| 4G 注入秒级 int timestamp | PASS | timestamp 为数值: True |
| 非 4G（WiFi）省略 timestamp | PASS | WiFi喇叭 正确省略 timestamp |
| 报文为压缩 JSON（无多余空白） | PASS | 载荷已压缩 |
| 离线判定仅 method=="close" | PASS | WillMethod=close:True, 识别 will:True, 正常消息非 will:True |
| 剧本方法均入 Catalog 且 4G 开关支持 | PASS | 全部 10 个剧本方法均在 Catalog 中且 4G 开关支持 |

## 三、监听阶段（上行抓取）

连接: **成功** — clientId=fieldtest_3788077b

监听到 IMEI 共 **0** 个：

| IMEI | 上行条数 |
| --- | --- |
| （无） | 0 |

## 四、命令下发结果

_未下发任何命令（监听模式或无设备）。_
## 五、非应答上行（事件 / Will / 主动上报）

_无。_

## 六、Catalog 修正建议

### 6.1 硬冲突（必须修正）

_未发现硬冲突。_

### 6.2 未覆盖字段（建议 Catalog 扩展）

_实测应答字段均与 Catalog 一致，无新增建议。_

## 七、运行提示

```bash
# 第一轮（只读探查，不动真实开关；显式 IMEI 跳过发现，可加 --listen-sec 0 立即下发）:
dotnet run --project tools/AnShengFieldTest -- --imei <15位IMEI> --listen-sec 0

# 完整剧本（会物理动作真实开关，需用户确认后放行）:
dotnet run --project tools/AnShengFieldTest -- --imei <15位IMEI> --allow-config --allow-control
```

## 八、已验证设计决策（empirical）

> 以下结论由 team-lead 用 python+paho 在 **同一台 EMQX broker** 上独立回环验证得出，
> 与 Phase 1 重构的某项设计决定相互印证，记录于此供 Phase 2 评审。

### 8.1 重叠订阅 EMQX 不去重

- 同时挂载 `#`、`/iot/server/#`、`/iot/server/iot-board/+` 三个重叠过滤器时，**同一条报文被投递了 3 次**。
- 说明该 EMQX 实例**不对重叠订阅做去重**：订阅多少次、同一消息就投递多少次。

### 8.2 「同 pattern 只订阅一次」是必需优化，不是可选

- Phase 1 重构决定：当 `WillTopicPattern == PublishTopicPattern` 时，**只订阅一次**（而非 will / publish 各订阅一次）。
- 本实证结论支持该决定：**若是当初为 will 与 publish 各订阅一次（同一 pattern），线上每条设备报文都会被处理两遍**
  → 重复入库、规则引擎重复执行、告警重复触发。
- 这同时也是 QA 的 **Q21（「同 pattern 只订阅一次的优化要重审」）** 的答案：**该优化是必需的，不是可选的。**

### 8.3 第一轮 0 上行的根因（非工具问题）

- 全量 `#` 根通配订阅 75s 仍为 0 条报文：整 broker 无流量，故 topic 形态并非本次 0 上行原因。
- ACL 无限制：上述四个过滤器 SUBACK 均为 `granted`，无 0x80 拒绝。
- 回环自测通过（自己 publish 自己收），订阅链路 100% 健康。
- 结论：**设备真的一条没发**——可能未上电/未联网，或在线但自动上报关闭（仅被轮询才应答）。
- 阻塞点：下行 topic 需要 IMEI。已支持 `--imei` 显式指定跳过发现，拿到 IMEI 即可开跑。
