# 安圣二开设备 MQTT 协议重构 — P3/P4/P5 验收状态核查报告

- **核查日期**：2026-08-03
- **核查人**：QA 工程师 严过关
- **核查性质**：只读审计（未修改任何生产代码）
- **权威依据**：`.workbuddy/design/ansheng-open-redesign.md` §6 任务分解（T1–T14）、`task-dependency.mermaid`
- **核查对象**：T1–T14 编号体系下的 Phase 3 / Phase 4 / Phase 5，并顺带核查其前置 Phase 2
- **代码基线**：`dotnet build IoTPlatform.sln` → **0 错误 / 81 警告**（均为既有 CS86xx 空引用类告警，与本次核查无关）

---

## 0. 一句话结论

> **P3、P4、P5 全部未开工（0/7 任务交付）。其前置 P2（T5–T7）同样未开工。**  
> 当前代码库中这些任务的痕迹**只存在于 T1 建立的 `AnShengCommandCatalog` 声明层和 T2 建立的 `AnShengCommandBuilder` 报文构造层**，  
> 没有任何一个任务具备「Service 方法 + Controller 端点 + 上行解析/Handler + 前端入口 + 测试覆盖」的完整业务链路。
>
> **最高优先级发现**：T4 前端清理欠账已经造成**线上可见的坏功能** —— 前端仍有 4 个伪命令按钮，  
> 其中 3 个调用已被物理删除的后端端点（必然 404），第 4 个 `getSwitchConfig` 更严重：  
> 它会被后端当作 Legacy 充电桩命令**真实下发一条协议中不存在的报文到设备**。详见 §3.2。

---

## 1. 结论速览

### 1.1 Phase 级结论

| Phase  | 任务      |              状态              | 一句话依据                                                                                                                                                       |
| ------ | ------- | :--------------------------: | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **P2** | T5–T7   |           ❌ **未开工**          | 设计要求新增的 25 个文件（Profile/事件管线/在途表）**一个都不存在**；`AnShengController` 仍是 6 月的 6 个端点，无 `/catalog`、`/profile`、`/commands/{id}`                                       |
| **P3** | T8–T9   |           ❌ **未开工**          | `AnShengSwitchController.cs`、`AnShengDelayTask.cs`、`AnShengScheduleService.cs` 均不存在；`Web/src/app/features/` 目录本身不存在                                         |
| **P4** | T10–T11 |           ❌ **未开工**          | `AnShengTimeTask.cs`、`AnShengEmStatistic.cs`、`AnShengEnergyService.cs`、`AnShengScheduleController.cs`、`AnShengEnergyController.cs` 均不存在；定时任务连 Builder 方法都没有 |
| **P5** | T12–T14 | 🟡 **仅 T14 部分完成（且系 T2 副产品）** | `AnShengMaintenanceController.cs` 不存在、`setMqtt/getMqtt` 仅 Catalog 声明；T14 的 Legacy 隔离逻辑已在 T2 阶段以内联方式实现，但未按设计落到 `Legacy/` 目录、无 `ProtocolFamily` 标记、无前端分区      |

### 1.2 任务级结论

| 任务                    | 设计文件数 |   实际新增  |         状态        | 一句话依据                                                                                                                                                                             |
| --------------------- | :---: | :-----: | :---------------: | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **T5** 设备能力档案 Profile |   6   |    0    |       ❌ 未开工       | `Models/AnShengDeviceProfile.cs` 不存在；`DiscoveredAnShengDevice.cs` 无 Kind/SlotAmount/Version/Iccid/ProbeStatus 字段；全仓 `InferKind`/`ProbeStatus`/`KindSource` 零命中                    |
| **T6** 事件识别与处理管道      |   12  |    0    |       ❌ 未开工       | `AnShengMessageRouter.cs`/`AnShengDataNormalizer.cs`/`AnShengEventDispatcher.cs`/`Services/AnShengEventHandlers/` 全部不存在；无 `AnShengDeviceEvent` 模型与表                               |
| **T7** 命令服务重构         |   7   |    0    |       ❌ 未开工       | 无 `IAnShengPendingCommandStore`/`AnShengPendingCommandStore`/`AnShengCommandRecord`；Controller 无 `/catalog`、`/profile`、`/commands/{commandId}`                                    |
| **T8** 开关动作与延时任务（后端）  |   5   |    0    |       ❌ 未开工       | Builder 有 `BuildAction`/`BuildActions`/延时三方法，但 Service/Controller/DTO/Model **零业务实现**（仅注释提及）                                                                                      |
| **T9** 开关控制面板（前端）     |   4   |    0    |       ❌ 未开工       | `SwitchControlPanel.tsx`/`DeviceEventTimeline.tsx` 不存在；`Web/src/app/features/` 目录不存在                                                                                              |
| **T10** 定时任务（前后端）     |   7   |    0    |       ❌ 未开工       | `setTimeTasks`/`getTimeTasks`/`getSlotTimeTasks`/`setSlotTimeTasks` **仅在 Catalog 声明**，连 Builder 专用方法都没有；Services/Controllers/DTOs/Models/Web 全部零命中                                |
| **T11** 电量实时/统计/校准    |   7   |    0    |       ❌ 未开工       | `AnShengEmStatistic.cs`/`AnShengEnergyService.cs`/`AnShengEnergyController.cs` 不存在；`getEMStatistics`/`clearEMStatistics` 在业务层零命中                                                  |
| **T12** 运维与配置命令       |   8   |    0    |       ❌ 未开工       | `AnShengMaintenanceController.cs`/`AnShengKeyConfig.cs`/`AnShengSimCheckConfig.cs`/`AnShengRs485Record.cs` 均不存在；`setTime`/`getLogs`/`send485`/`getKeyConfig`/`getSimCheck` 业务层零命中 |
| **T13** MQTT 参数远程配置   |   4   |    0    |       ❌ 未开工       | `setMqtt`/`getMqtt` 全仓仅 `AnShengCommandCatalog.cs:200-204` 两处声明，Service/Controller/DTO/Web 零命中 —— **与 7-31 日志「属 Phase 5，暂未实现」的记载一致**                                              |
| **T14** 协议族隔离 Legacy  |   4   | 0（内联实现） |      🟡 部分完成      | Legacy 分流已在 `AnShengCommandBuilder.cs:447-550` + `AnShengMqttProtocolAdapter.cs:350-360` 实现且有效，但未落 `Legacy/` 目录、Catalog 无 `ProtocolFamily` 字段、前端无协议族分区                            |
| **T4** 删除伪命令（前端）※前置欠账 |   6   |    0    | ❌ 未开工（**已造成坏功能**） | `Web/src/app/pages/AnShengManagementPage.tsx:119-161` 仍有 4 个伪命令模板，`anshengApi.ts:72-100` 仍有 3 个指向已删端点的 API                                                                        |

### 1.3 日志记载 vs 代码实际 —— 一致性核对

| 日志记载（2026-07-31）       | 代码实证                                                                                                                                      |               判定              |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | :---------------------------: |
| P1（T1+T2+T3）已完成        | `Infrastructure/Protocol/AnSheng/` 下 7 个新文件齐全；后端伪命令仅剩解释性注释                                                                                |            ✅ **属实**           |
| 4 个伪命令后端痕迹已物理删除        | 后端 5 处命中**全部为注释**（`AnShengCommandService.cs:264`、`IAnShengCommandService.cs:40-41`、`AnShengController.cs:378`、`AnShengRequests.cs:68-69`） |            ✅ **属实**           |
| T4 前端清理刻意延后            | 前端伪命令**原样保留且仍被调用**                                                                                                                        |    ✅ 属实，但**风险被低估**（见 §3.2）    |
| setMqtt 属 Phase 5，暂未实现 | 仅 Catalog 声明，无任何实现                                                                                                                        |            ✅ **属实**           |
| 「依联调结果决定 Phase 2」      | P2 三任务**均无任何代码**                                                                                                                          |        ✅ 属实（即 P2 从未启动）        |
| 测试 402/402 通过          | 测试工程存在，2 文件 2230 行；**但仅覆盖协议层**（Catalog/Builder/Parser），对 Service/Controller/DbContext **零覆盖**                                             | 🟡 **数字属实，但覆盖范围需澄清**（见 §2.11） |

> **结论：本次核查未发现「日志声称已完成、代码实际未做」的虚报情况。**  
> 日志的问题不在于虚报，而在于 **P1 之后的 T4 欠账风险描述不足** —— 日志把它描述为「避免空窗期」的主动选择，  
> 但实际后果是「前端有 4 个必然失败甚至会污染设备的按钮」，属于已生效的线上缺陷而非中性等待状态。

---

## 2. 逐任务核查明细

> 判定口径（三级递进，本次核查的核心区分）：
>
> 1. **Catalog 里有定义** —— T1 已把 36 个 method 全部录入，**这不等于功能已实现**
> 2. **Builder 能构造报文** —— T2 的产物，**也不等于业务链路完整**
> 3. **Service 方法 + Controller 端点 + 上行解析/Handler + 前端入口 + 测试覆盖** —— 才算真正交付
>
> 下文每个任务均标注其停留在第几级。

### 2.1 T5 — 设备能力档案（Profile）与品类判定 ❌ 未开工

**设计要求交付物**（§6 Phase 2，6 文件）：  
`Models/AnShengDeviceProfile.cs`🆕 / `Services/AnShengDeviceProfileService.cs`🆕 / `Services/Interfaces/IAnShengDeviceProfileService.cs`🆕 /  
`Models/DiscoveredAnShengDevice.cs`✏️（+Kind/SlotAmount/Version/Iccid/ProbeStatus）/ `Services/AnShengDiscoveryService.cs`✏️ / `Data/AppDbContext.cs`✏️ + Migration

**代码库实际**：

- **Profile 三件套全部不存在**
  ```
  $ ls Services/ | grep -i ansheng
  AnShengCommandService.cs
  AnShengDiscoveryService.cs          ← 仅此两个，无 AnShengDeviceProfileService.cs
  $ ls Models/ | grep -i ansheng
  AnShengDeviceConfig.cs
  DiscoveredAnShengDevice.cs          ← 无 AnShengDeviceProfile.cs
  ```
- **`DiscoveredAnShengDevice.cs` 未扩展任何字段**（全文 45 行，字段止于 6 月版本）：
  ```csharp
  // Models/DiscoveredAnShengDevice.cs:26-44
  public string Imei { get; set; }        // 26
  public string? Model { get; set; }      // 30
  public string? NetType { get; set; }    // 33
  public DateTime DiscoveredAt { get; set; }   // 36
  public DateTime? LastSeenAt { get; set; }    // 38
  public bool IsClaimed { get; set; }          // 41
  public long? ClaimedDeviceId { get; set; }   // 44
  ```
  → 设计要求的 `Kind` / `SlotAmount` / `Version` / `Iccid` / `ProbeStatus` **五个字段一个都没有**。
- **全仓关键标识零命中**：
  ```
  $ grep -rnE "DeviceProfile|InferKind|ProbeStatus|KindSource|SlotAmount" Services/ Models/ Controllers/ --include=*.cs
  (无输出)
  ```
- **无新增 Migration**：`Migrations/` 下最后一个安圣相关迁移是 `20260627085758_AddAnShengIntegration.cs`（6-27，属旧 Phase），  
  7-31 之后**无任何新迁移**。`Data/AppDbContext.cs:70-71` 仅两个安圣 DbSet：
  ```csharp
  public DbSet<AnShengDeviceConfig> AnShengDeviceConfigs { get; set; }        // 70
  public DbSet<DiscoveredAnShengDevice> DiscoveredAnShengDevices { get; set; } // 71
  ```
- **品类判定的现状是「内存缓存 + 无持久化」**，这是 T5 缺位的直接后果：
  ```csharp
  // Infrastructure/Protocol/Adapters/AnShengMqttProtocolAdapter.cs:105-109
  public static AnShengDeviceKind GetDeviceKind(string? imei)
  {
      if (string.IsNullOrWhiteSpace(imei)) return AnShengDeviceKind.Unknown;
      return DeviceKinds.TryGetValue(imei, out var kind) ? kind : AnShengDeviceKind.Unknown;
  }
  ```
  `DeviceKinds` 是**静态内存字典**，由上行报文学习（`:545` 判空跳过、`:569` 写入）。**进程重启即全部丢失**。

**验收标准逐条核对**：

|  #  | 验收标准                                              |         结果         |
| :-: | ------------------------------------------------- | :----------------: |
|  1  | 迁移可正向执行与回滚                                        |        ❌ 无迁移       |
|  2  | 认领后 Profile 的 SlotAmount/Version/NetType/Kind 均非空 |    ❌ 无 Profile 表   |
|  3  | `InferKind` 三项单测                                  | ❌ 无 `InferKind` 实现 |
|  4  | 探测失败时 `ProbeStatus=ProbeFailed`                   |   ❌ 无 ProbeStatus  |

**判定：❌ 未开工**（0/6 文件，0/4 验收标准）

---

### 2.2 T6 — 事件识别与处理管道 ❌ 未开工

**设计要求交付物**（12 文件）：  
`AnShengMessageRouter.cs`🆕 / `AnShengDataNormalizer.cs`🆕 / `Services/AnShengEventDispatcher.cs`🆕 / `IAnShengEventHandler.cs`🆕 /  
`Services/AnShengEventHandlers/{Connected,Close,Key,Delay,Time,Recv485,SimCheck}EventHandler.cs`🆕（7 个）/ `Models/AnShengDeviceEvent.cs`🆕 /  
`ProtocolConfigService.cs`✏️ / `DataCollectionService.cs`✏️ / `AppDbContext.cs`✏️ + Migration

**代码库实际**：

- **目录与文件全部不存在**：
  ```
  $ ls Services/AnShengEventHandlers/
  ls: cannot access 'Services/AnShengEventHandlers/': No such file or directory

  $ ls Infrastructure/Protocol/AnSheng/
  AnShengCommandBuilder.cs   AnShengCommandCatalog.cs   AnShengCommandSpec.cs
  AnShengCommandThrottle.cs  AnShengDeviceKind.cs       AnShengFirmwareVersion.cs
  AnShengMessageParser.cs    AnShengMessageTypes.cs     AnShengParamSpec.cs
  AnShengTimestampConverter.cs  AnShengWillEventArgs.cs
  ← 无 AnShengMessageRouter.cs、无 AnShengDataNormalizer.cs
  ```
- **`AnShengDeviceEvent` 模型与表不存在**（`grep -rn "AnShengDeviceEvent" Services/ Models/` 零命中）。
- **仅有的「事件」痕迹是 T1/T2 的分类枚举，不是处理管线**：
  ```csharp
  // Infrastructure/Protocol/AnSheng/AnShengMessageParser.cs:126
  "connected" or "keyEvent" or "delayEvent" or "timeEvent" or "recv485"
  ```
  这是 Parser 的**分类判定**（第 1 级），下游没有 Router、没有 Dispatcher、没有 Handler 责任链、不落库、不投递规则引擎。

**验收标准逐条核对**：6 条验收标准（七种报文 Classify、frameId 在途判定、delayEvent 判 Event、事件落库+规则引擎、close 30s 去抖、`slot{n}_*` 映射）  
**全部无法满足** —— 其中「Classify 返回 Event」在 Parser 层部分具备，但设计要求的 `AnShengMessageRouter.Classify` 三分支（Response/AutoReport/Event）不存在。

**判定：❌ 未开工**（0/12 文件，0/6 验收标准）

---

### 2.3 T7 — 命令服务重构（校验 + 在途表 + 命令记录 + Catalog API）❌ 未开工

**设计要求交付物**（7 文件）：  
`AnShengCommandService.cs`✏️ / `IAnShengCommandService.cs`✏️ / `IAnShengPendingCommandStore.cs`🆕 / `AnShengPendingCommandStore.cs`🆕 /  
`Models/AnShengCommandRecord.cs`🆕 / `AnShengController.cs`✏️（+`GET /catalog`、`GET /{id}/profile`、`GET /commands/{commandId}`）/ `AppDbContext.cs`✏️ + Migration

**代码库实际**：

- **在途表与命令记录不存在**：`grep -rn "PendingCommand|CommandRecord" Services/ Models/` 在安圣相关文件零命中  
  （仅 `DeviceCommandService.cs` 有同名概念，属**通用设备命令**模块，非安圣专用）。
- **Controller 端点仍是 6 月的 6 个，无一个 T7 新端点**：
  ```
  // Controllers/AnShengController.cs   [Route("api/v1/ansheng")]  :21
  :49   [HttpPost("discover")]
  :68   [HttpGet("discovered")]
  :145  [HttpPost("claim")]
  :274  [HttpPost("{deviceId:long}/auto-report")]
  :349  [HttpPost("{deviceId:long}/command")]
  :388  [HttpPost("{deviceId:long}/reboot")]
  ```
  → **无 `GET /catalog`**（T7 验收标准 6 直接失败，同时阻塞 T4 的 Catalog 驱动控制台）  
  → **无 `GET /{id}/profile`**、**无 `GET /commands/{commandId}`**（T9 验收标准 2「按 commandId 轮询」无端点可用）  
  → 另注：**也无 `GET /{id}/status`** —— 即 T3 注释中引导的「请改用 GET /api/ansheng/{id}/status」这个替代端点**并不存在**。
- **接口未做任何 T7 改造**（`Services/Interfaces/IAnShengCommandService.cs` 全文 45 行，仍是 6 月的 4 个方法）：  
  `SendCommandAsync` / `ConfigureAutoReportAsync` / `TriggerDiscoveryAsync` / `RebootDeviceAsync`。
- **已具备的部分：Catalog 层校验（属 T1/T2 溢出，非 T7 交付）**
  ```csharp
  // Services/AnShengCommandService.cs:104-127
  if (AnShengCommandCatalog.TryGet(method, out var spec) && spec != null)
  {
      if (spec.IsEvent)                    // :106  事件不可下发
          return ... "是设备上报事件，平台不可下发";
      if (!spec.IsSupportedBy(kind))       // :113  品类校验
          return ... $"{kind.ToDisplayName()} 不支持命令 {method}";
      var validation = spec.ValidateParams(parameters);   // :120  参数 schema 校验
      if (!validation.IsValid) return ... ;
  }
  ```
- **⚠️ 但该校验当前是「fail-open」且不可靠**，这是 T5 缺位导致的连带缺陷：
  ```csharp
  // Infrastructure/Protocol/AnSheng/AnShengCommandSpec.cs:50-56
  /// <param name="kind">Unknown 视为「无法判定」，返回 true 以免误拦截。</param>
  public bool IsSupportedBy(AnShengDeviceKind kind)
  {
      if (kind == AnShengDeviceKind.Unknown) return true;   // ← 放行一切
      return (SupportedKinds & kind.ToCapability()) != AnShengDeviceCapability.None;
  }
  ```
  结合 `GetDeviceKind` 的内存字典特性（§2.1）：**服务重启后、或设备尚未上报过，所有设备 Kind = Unknown → 品类校验全部放行**。  
  → T7 验收标准 1「对 SpeakerWiFi 发 `action` → RejectedByKind，MQTT 无任何发布」**在当前实现下不可靠地成立**（取决于进程内是否恰好学习过该 IMEI）。

**验收标准逐条核对**：

|  #  | 验收标准                                            |                      结果                      |
| :-: | ----------------------------------------------- | :------------------------------------------: |
|  1  | SpeakerWiFi 发 action → RejectedByKind           | 🟡 逻辑存在但 Unknown 时 fail-open，无持久化 Profile 兜底 |
|  2  | slotAmount=4 发 slotNum:9 → RejectedByValidation |   ❌ 无 Profile 即无 slotAmount，日志已列为 KnownGap   |
|  3  | 固件 4.0.8 发 `q` 参数的处理                            |                   ❌ 无固件持久化                   |
|  4  | 两设备同 frameId 应答互不串扰                             |        ❌ 无在途表（key `{imei}:{frameId}`）        |
|  5  | 30s 无应答 → Status=Timeout                        |                ❌ 无命令记录表、无超时清扫                |
|  6  | `GET /catalog` 返回 36 条                          |                    ❌ 端点不存在                   |

**判定：❌ 未开工**（0/7 交付文件；仅 Catalog 校验以 T1 溢出形式部分存在，0/6 验收标准完整满足）

---

### 2.4 T8 — 开关动作与延时任务（后端）❌ 未开工 【P3 核心】

**设计要求交付物**（5 文件）：  
`Controllers/AnShengSwitchController.cs`🆕 / `Models/AnShengDelayTask.cs`🆕 / `Services/AnShengScheduleService.cs`🆕 /  
`DTOs/Requests|Responses/AnSheng*.cs`✏️ / `AppDbContext.cs`✏️ + Migration

**代码库实际** —— 典型的「停在第 2 级」：

**✅ 第 1 级 Catalog 有定义**：

```csharp
// Infrastructure/Protocol/AnSheng/AnShengCommandCatalog.cs
:210  Spec("action",  "插槽开关动作",   AnShengDeviceCapability.GroupSwitchAction, ...)
:216  Spec("actions", "多插槽开关动作", AnShengDeviceCapability.GroupSwitchAction, ...)
:222  Spec("getDelayTasks",  "获取延时任务列表", AnShengDeviceCapability.GroupSwitchAction, ...)
:225  Spec("startDelayTask", "开始延时任务",     AnShengDeviceCapability.GroupSwitchAction, ...)
:235  Spec("stopDelayTask",  "停止延时任务",     AnShengDeviceCapability.GroupSwitchAction, ...)
:239  Event("delayEvent",    "延时任务事件上报", AnShengDeviceCapability.GroupSwitchAction, ...)
```

**✅ 第 2 级 Builder 能构造报文**：

```csharp
// Infrastructure/Protocol/AnSheng/AnShengCommandBuilder.cs
:324-339  BuildAction(...)   → BuildCommand(imei, "action", parameters, kind)
:347-362  BuildActions(...)  → BuildCommand(imei, "actions", parameters, kind)
:366-379  startDelayTask     → BuildCommand(imei, "startDelayTask", ...)
:389-397  stopDelayTask      → BuildCommand(imei, "stopDelayTask", ...)
:403-410  getDelayTasks      → BuildCommand(imei, "getDelayTasks", null, kind)
```

**❌ 第 3 级业务链路完全缺失** —— Services / Controllers / DTOs / Models / Data 中的命中**全部是注释**：

```
$ grep -rnE "startDelayTask|stopDelayTask|getDelayTasks|delayEvent" Services Controllers DTOs Models Data --include=*.cs
(无输出 —— 一个都没有，连注释都没有)

$ grep -rnE '"action"|"actions"' Services Controllers DTOs Models --include=*.cs
Services/AnShengCommandService.cs:266:    //     开关通断请改用 SendCommandAsync(deviceId, "action", { slotNum, action })。   ← 注释
Services/Interfaces/IAnShengCommandService.cs:39: // 开关通断请使用 SendCommandAsync(...)                                              ← 注释
Controllers/AnShengController.cs:380: //  POST /command  { "method": "action",  "parameters": {...} }                          ← 注释
DTOs/Requests/AnShengRequests.cs:70:  //  { "method": "action",  "parameters": { "slotNum": 1, "action": "on" } }             ← 注释
```

**缺失清单**：

- `Controllers/AnShengSwitchController.cs` **不存在** → 无 `POST /api/ansheng/{id}/action`、无 `POST /actions`
- `Models/AnShengDelayTask.cs` **不存在** → 无延时任务镜像表
- `Services/AnShengScheduleService.cs` **不存在** → 无「写后回读（≥100ms）」、无 `SyncedAt` 维护
- `delayEvent` Handler **不存在**（依赖未开工的 T6）→ 镜像无法被事件更新

**当前唯一可用路径**：通用端点 `POST /api/v1/ansheng/{deviceId}/command`（`AnShengController.cs:349`）+ `{"method":"action"}`。  
即「**能把报文发出去，但没有任何开关业务语义**」：无镜像、无回读、无 `slots` 快照、无幂等、无专用 DTO 校验。

**验收标准逐条核对**：

|  #  | 验收标准                                                      |                     结果                    |
| :-: | --------------------------------------------------------- | :---------------------------------------: |
|  1  | `POST /{id}/action` 报文与 §1 表 12 一致，应答 slots 写入 Profile 快照 |            ❌ 端点不存在；Profile 不存在            |
|  2  | `POST /actions {slotNums:[1,3]}` 正确构造数组                   |    🟡 Builder 可构造（`:347-362`），但**无端点**    |
|  3  | startDelayTask 成功后自动触发 getDelayTasks，SyncedAt 更新          |          ❌ 无 ScheduleService、无镜像表         |
|  4  | 注入 delayEvent → 镜像 Enable=false                           |              ❌ 无 Handler、无镜像表             |
|  5  | 喇叭类调用 → 400 + RejectedByKind                              | ❌ 端点不存在（通用端点返回 200 包 Success=false，非 400） |

**判定：❌ 未开工**（0/5 文件，0/5 验收标准；仅 Catalog+Builder 两级就绪）

---

### 2.5 T9 — 开关控制面板（前端）❌ 未开工 【P3 核心】

**设计要求交付物**（4 文件）：  
`Web/src/app/features/ansheng/components/SwitchControlPanel.tsx`🆕 / `DeviceEventTimeline.tsx`🆕 / `anshengApi.ts`✏️ / `ansheng.types.ts`✏️

**代码库实际**：

```
$ ls Web/src/app/features/ansheng/
ls: cannot access 'Web/src/app/features/ansheng/': No such file or directory

$ ls Web/src/app/features/
(目录本身不存在)

$ grep -rl "SwitchControlPanel|DeviceEventTimeline" Web/src
(无输出)
```

全部安圣前端资产仅 3 个文件，且均为 6 月旧版：  
`Web/src/app/pages/AnShengManagementPage.tsx` / `Web/src/app/services/api/anshengApi.ts` / `Web/src/app/services/api/types/ansheng.types.ts`

**验收标准**：3 条（按 slotAmount 渲染 N 路、按 commandId 轮询、喇叭类不显示面板）**全部不满足**，且均硬阻塞于未开工的 T5（slotAmount）与 T7（commandId 查询端点）。

**判定：❌ 未开工**（0/4 文件，0/3 验收标准）

---

### 2.6 T10 — 定时任务（后端 + 前端）❌ 未开工 【P4】

**设计要求交付物**（7 文件）：  
`Models/AnShengTimeTask.cs`🆕 / `AnShengScheduleService.cs`✏️（补定时部分）/ `Controllers/AnShengScheduleController.cs`🆕 /  
`Web/.../ScheduleEditor.tsx`🆕 / `DTOs`✏️ / `AppDbContext.cs`✏️ + Migration

**代码库实际** —— 本任务**连第 2 级都没到**，是所有任务中最"空"的：

**✅ 第 1 级 Catalog 有定义**：

```csharp
// Infrastructure/Protocol/AnSheng/AnShengCommandCatalog.cs
:261  Spec("getTimeTasks",     "获取所有定时任务",         AnShengDeviceCapability.GroupTimeTask, ...)
:264  Spec("setTimeTasks",     "设置所有定时任务",         AnShengDeviceCapability.GroupTimeTask, ...)
:269  Spec("getSlotTimeTasks", "获取单个插槽/开关定时任务", AnShengDeviceCapability.GroupTimeTask, ...)
:272  Spec("setSlotTimeTasks", "设置单个插槽/开关定时任务", AnShengDeviceCapability.GroupTimeTask, ...)
```

**❌ 第 2 级 Builder 无专用方法**：`AnShengCommandBuilder.cs` 中 `grep "TimeTasks"` **零命中**  
（只能走通用 `BuildCommand(imei, method, params, kind)`）。

**❌ 第 3 级完全空白**：

```
$ grep -rnE "setSlotTimeTasks|getSlotTimeTasks|setTimeTasks|getTimeTasks|timeEvent" \
    Services Controllers DTOs Models Data --include=*.cs
(无输出)

$ grep -rnE "setSlotTimeTasks|getSlotTimeTasks|setTimeTasks|getTimeTasks|timeEvent" Web/src
(无输出)
```

→ `Models/AnShengTimeTask.cs`、`Controllers/AnShengScheduleController.cs`、`ScheduleEditor.tsx` 均不存在；无 `RowVersion` 乐观并发字段；无 `timeEvent` Handler。

**验收标准**：6 条（仅 Switch4G 放行、Confirm=true 二次确认、保存后回读、timeEvent 就地更新、409 并发冲突、24h 过期提示）**全部不满足**。

> 补充：设计文档 §7.1 **R3 原将 `getSlotTimeTasks`/`setSlotTimeTasks` 标为「阻塞」**（参数表缺 `slotNum`）。  
> **已于 2026-08-03 经安圣正式确认含 `slotNum` 参数**（`setSlotTimeTasks.slotNum` 必填 int 1 起，`getSlotTimeTasks.slotNum` 可选），R3 关闭，见设计文档 §7 关闭标记及 L124/L125/L1569。T10 单插槽版与整表版均需完整实现。

**判定：❌ 未开工**（0/7 文件，0/6 验收标准；仅 Catalog 一级就绪）

---

### 2.7 T11 — 电量计实时、统计与校准 ❌ 未开工 【P4】

**设计要求交付物**（7 文件）：  
`Models/AnShengEmStatistic.cs`🆕 / `Services/AnShengEnergyService.cs`🆕 / `Controllers/AnShengEnergyController.cs`🆕 /  
`Web/.../EnergyStatisticsPanel.tsx`🆕 / `DataCollectionService.cs`✏️ / `DTOs`✏️ / `AppDbContext.cs`✏️ + Migration

**代码库实际**：

- **Catalog 14 处命中、Builder 2 处命中，业务层 0 处**：
  ```
  $ grep -rcE "getEMRealtime|getEMStatistics|clearEMStatistics|getCalParams|setCalParams|resetCalParams|autoCal" \
      Infrastructure --include=*.cs | grep -v ":0"
  Infrastructure/Protocol/AnSheng/AnShengCommandCatalog.cs:14
  Infrastructure/Protocol/AnSheng/AnShengCommandBuilder.cs:2

  $ grep -rnE "getEMRealtime|getEMStatistics|clearEMStatistics|getCalParams|setCalParams|resetCalParams|autoCal" \
      Services Controllers DTOs Models Data --include=*.cs
  (无输出)
  ```
- **唯一的前端痕迹是一个孤立的命令模板**（无配套面板）：
  ```tsx
  // Web/src/app/pages/AnShengManagementPage.tsx:58
  method: 'getEMRealtime',
  ```
  → 它走通用 `sendCommand`，能发出报文，但**应答不入库、不聚合、无展示**。
- `Models/AnShengEmStatistic.cs`、`Services/AnShengEnergyService.cs`、`Controllers/AnShengEnergyController.cs`、  
  `EnergyStatisticsPanel.tsx` **均不存在**；无聚合表、无唯一键幂等、无 `PeriodKey`。

**验收标准**：6 条（幂等、hourSumData 48 项、不产生空洞行、清零后数据保留、实时数据入 DeviceDataRecord、校准仅开关类）**全部不满足**。

> 补充：设计文档 §7.1 **R8 已于 2026-08-03 关闭**——安圣确认 `getEMStatistics.data[]` 按插槽 1~n 顺序（`SlotNum = index + 1`），仍保留 `Profile.SlotAmount` 校验数组长度（依赖未开工的 T5）。

**判定：❌ 未开工**（0/7 文件，0/6 验收标准）

---

### 2.8 T12 — 运维与配置命令 ❌ 未开工 【P5】

**设计要求交付物**（8 文件）：  
`Controllers/AnShengMaintenanceController.cs`🆕 / `Models/AnShengKeyConfig.cs`🆕 / `AnShengSimCheckConfig.cs`🆕 / `AnShengRs485Record.cs`🆕 /  
`Models/AnShengDeviceConfig.cs`✏️（补全 setAutoReport 字段）/ `AnShengMqttProtocolOptions.cs`✏️（多组 topic）/ `DTOs`✏️+`Web`✏️ / `AppDbContext.cs`✏️ + Migration

**代码库实际**：

- **`AnShengMaintenanceController.cs` 不存在**；`Controllers/` 下安圣控制器只有 `AnShengController.cs` 一个。
- **三个新模型均不存在**：`AnShengKeyConfig.cs` / `AnShengSimCheckConfig.cs` / `AnShengRs485Record.cs`。
- **业务层命令零实现**：
  ```
  $ grep -rnE "setTime|getLogs|send485|getKeyConfig|setKeyConfig|getSimCheck|setSimCheck" \
      Services Controllers Models --include=*.cs   (排除注释后)
  (无输出)
  ```
- **`AnShengDeviceConfig.cs` 未补全字段**（全文字段仅 4 个业务字段，属 6 月版本）：
  ```csharp
  // Models/AnShengDeviceConfig.cs:30-40
  public int? GetDevStatusSec { get; set; }   // 30
  public string? GetDevStatusQ { get; set; }  // 34
  public int? OrderUpSec { get; set; }        // 37
  public int? Rs485Sec { get; set; }          // 40
  ```
  → 设计明确要求的 **`rs485BaudRate`（必填）缺失**，T12 验收标准 1「setAutoReport 下发报文含全部必填字段」不满足。
- **两个已具备的能力，但均属 T2/旧 Phase 产物而非 T12 交付**：
  - `reboot` 走正确路径：`Services/AnShengCommandService.cs:271` → `SendCommandAsync(deviceId, "reboot", null, ct)` → 经 Catalog 分支构造扁平报文。此项符合 T12「reboot 改走正确路径」意图。
  - `setAutoReport` 存在：`AnShengCommandService.cs:195`、端点 `AnShengController.cs:274`，但字段不全（见上）。
- **多组 topic 未实现**：`AnShengMqttProtocolOptions.cs` 仍为单组模板；7-31 日志已判定「T12 的多组 topic 共存可延后」，与代码一致。
- **`isBeta` 标记已在 Catalog 就绪**（`AnShengCommandCatalog.cs:177/180/294/304`，覆盖 getAutoReport/setAutoReport/send485/recv485），  
  但**前端未消费**（无 CommandConsole，无「测试中」角标）→ T12 验收标准 5 不满足。

**验收标准**：5 条中 **0 条完整满足**（标准 2 的 `setTime` 秒级时间戳能力在 Builder 层具备，但无端点、无 WiFi 品类拒绝路径）。

**判定：❌ 未开工**（0/8 文件；仅 reboot 路径与 isBeta 标记以 T1/T2 溢出形式具备）

---

### 2.9 T13 — MQTT 参数远程配置（高危）❌ 未开工 【P5】

**设计要求交付物**（4 文件）：  
`AnShengMaintenanceController.cs`✏️ / `DTOs/Requests/AnShengRequests.cs`✏️ / `Web/.../MqttParamsEditor.tsx`🆕 / `AnShengCommandService.cs`✏️（高危审计）

**代码库实际** —— 全仓 `setMqtt`/`getMqtt` 仅 **2 处**，均在 Catalog：

```csharp
// Infrastructure/Protocol/AnSheng/AnShengCommandCatalog.cs
:200  Spec("getMqtt", "获取MQTT参数", AnShengDeviceCapability.GroupMqtt, "获取MQTT参数（getMqtt）"),
:203  Spec("setMqtt", "设置MQTT参数", AnShengDeviceCapability.GroupMqtt, "设置MQTT参数（setMqtt）", ...)
```

```
$ grep -rnE "setMqtt|getMqtt" Services Controllers DTOs Models Data --include=*.cs
(无输出)
$ grep -rnE "setMqtt|getMqtt" Web/src
(无输出)
```

→ 无 `ConfirmToken` 校验、无管理员鉴权、无 `subscribeTopic != publishTopic` 本地校验、无 `password` 掩码、无审计日志、无 `MqttParamsEditor.tsx`。

**验收标准**：5 条全部不满足。

> **与日志一致性**：7-31 日志「用户决策 #3：`setMqtt` 仅超级管理员可用（属 Phase 5，暂未实现）」——  
> **代码实证与日志记载完全一致，无偏差**。  
> 另注：设计文档 §7.2 **R1 提出「若用户不需要此功能，T13 可整体取消」**，用户决策为「仅超级管理员可用」即保留，故 T13 仍是待办而非已取消。

**判定：❌ 未开工**（0/4 文件，0/5 验收标准；仅 Catalog 声明）

---

### 2.10 T14 — 协议族隔离：充电桩 Legacy 命令归位 🟡 部分完成 【P5】

**这是 P3/P4/P5 中唯一有实质进展的任务，但进展来自 T2 的副产品，而非按 T14 设计执行。**

**设计要求交付物**（4 文件）：  
`Infrastructure/Protocol/AnSheng/Legacy/AnShengLegacyCommandBuilder.cs`🆕 / `AnShengMessageParser.cs`✏️ /  
`AnShengCommandService.cs`✏️ / `Web/.../CommandConsole.tsx`✏️

**已完成部分 ✅**：

- **Legacy 报文构造已隔离**（但内联在主 Builder 中，非独立文件）：
  ```csharp
  // Infrastructure/Protocol/AnSheng/AnShengCommandBuilder.cs
  :447  // ── Legacy 充电桩协议族（非二开协议，保留以兼容既有链路）
  :462  [Obsolete("Legacy 充电桩协议族专用，二开设备请使用 BuildCommand/BuildAction 等目录驱动方法")]
  :472  return BuildLegacy(imei, "orderStart", param);
  :485  => BuildLegacy(imei, "orderEnd", ...)
  :504  public (string FrameId, string Payload) BuildLegacyCommand(...)
  :526  /// Legacy 报文构建：保留 param 包裹与毫秒字符串 timestamp，确保充电桩链路行为不变。
  :532  private (string FrameId, string Payload) BuildLegacy(...)
  ```
- **下发侧按协议族分流已生效**：
  ```csharp
  // Infrastructure/Protocol/Adapters/AnShengMqttProtocolAdapter.cs:350-360
  if (AnShengCommandCatalog.Contains(commandType))
      (frameId, payloadJson) = _commandBuilder.BuildCommand(serialNumber, commandType, flatParams, kind);
  else
      // Legacy 充电桩协议族（orderStart / orderEnd 等）：保留 param 包裹，行为不变
      (frameId, payloadJson) = _commandBuilder.BuildLegacyCommand(serialNumber, commandType, flatParams);
  ```
- **解析侧双协议族已兼容**：
  ```csharp
  // Infrastructure/Protocol/AnSheng/AnShengMessageParser.cs
  :12   ///   2. Legacy 充电桩协议：业务字段位于 param 对象内。
  :122-124   "orderStart"/"orderEnd"/"orderUp" => AnShengMessageCategory.*
  :161  /// 解析 Legacy 充电桩订单消息（orderStart / orderEnd / orderUp）。
  :194  /// 取消息体 JSON：Legacy 报文取 param，二开报文取整条 RawJson。
  :200  #pragma warning disable CS0618 // Legacy 充电桩链路仍依赖 param
  ```
  → 对应 T14 验收标准 2「充电桩 `orderStart` 报文结构保持改造前完全一致」，7-31 日志亦记载「Legacy 充电桩链路完好未破坏」。

**未完成部分 ❌**：

| 设计要求                                                                       | 实际                                                                                                           |
| -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| 🆕 `Infrastructure/Protocol/AnSheng/Legacy/AnShengLegacyCommandBuilder.cs` | **`Legacy/` 目录不存在**，逻辑内联于 `AnShengCommandBuilder.cs:447-550`                                                 |
| Catalog 中为 Legacy 命令标记 `ProtocolFamily=ChargingPile`                       | **`ProtocolFamily` 字段在 `AnShengCommandSpec.cs` 中不存在**（该文件仅有 `IsBeta` 等，见 `:39`）；当前靠「不在 Catalog 中」隐式判定 Legacy |
| `Web/.../CommandConsole.tsx` 按协议族分区展示                                      | **文件不存在**（前端无任何协议族概念）                                                                                        |
| 验收 1：二开设备命令列表不出现 `orderStart/orderEnd/orderUp`                             | ❌ 前端命令模板为硬编码数组，无品类/协议族过滤                                                                                     |
| 验收 3：`close` 遗嘱在两协议族下都能正确处理                                                | 🟡 遗嘱判定已改 `method=="close"`（T2 完成），但**无双协议族回归测试**佐证                                                          |

> **隐式判定的脆弱性（值得记入风险）**：当前「不在 Catalog 中 ⇒ 按 Legacy 下发」是一个**兜底放行**策略。  
> 任何拼写错误或协议外的 method 都会被当作 Legacy **真实发往设备**，而不是快速失败。这正是 §3.2 中 `getSwitchConfig` 问题的成因。

**判定：🟡 部分完成**（后端隔离语义**有效**，约完成 50%；缺独立文件归位、`ProtocolFamily` 显式标记、前端分区）

---

### 2.11 测试覆盖核查

**测试工程现状**：

```
tests/IoTPlatform.AnSheng.Tests/
├── AnShengProtocolTests.cs            570 行   （工程师原有）
└── AnShengProtocolConformanceTests.cs 1660 行  （QA 交叉验证，断言取自 asopen.md 原文）
                                       2230 行合计
```

**覆盖边界**（本次核查的关键澄清）：

```
$ grep -rnE "AnShengCommandService|Controller|WebApplicationFactory|DbContext" tests/IoTPlatform.AnSheng.Tests/*.cs
(无输出)
```

→ **测试 100% 集中于协议层**（`AnShengCommandCatalog` / `AnShengCommandBuilder` / `AnShengMessageParser` / `AnShengFirmwareVersion`），  
**对 Service 层、Controller 层、EF DbContext 零覆盖**，也无 `WebApplicationFactory` 集成测试基础设施。

**这意味着**：

- 日志中「402/402 通过」的结论**真实且有效**，但其保护范围**仅限 T1/T2 的协议正确性**。
- T5–T14 的验收标准中大量为**集成测试**（事件落库、命令超时、镜像回读、并发 409、聚合幂等），  
  当前测试工程**不具备承载这类用例的基础设施**（无内存数据库/无 TestServer）。
- **P3 开工前需要先补一层集成测试脚手架**，否则 T8 之后的验收标准无法被自动化验证。

**已知缺口（日志已用 `KnownGap` 测试固化，本次核查确认仍然存在）**：

1. `AnShengParamSpec.MatchesType` 对 Object 型参数的 `Dictionary` 直传误杀
2. `slotNum` 上界未按设备 `slotAmount` 校验 —— **硬依赖 T5**
3. `uploadEnable` v5.0.1 固件门槛未在 `ValidateParams` 强制

延时任务/开关相关的测试**确实存在且质量高**，但只验证「报文构造正确」，例如：

```csharp
// tests/IoTPlatform.AnSheng.Tests/AnShengProtocolConformanceTests.cs
:220  [InlineData("startDelayTask", "eAction,enable,sAction,secs,slotNum")]
:345  [InlineData("getDelayTasks", "{\"method\":\"getDelayTasks\",\"frameId\":\"1745396239780\"}")]
:412  "method": "startDelayTask",     ← asopen.md:2085-2099 原文回放
:848  "method": "delayEvent",         ← asopen.md:2301-2317 原文回放
```

→ 这些是 **T1/T2 的验收资产**，**不能**作为 T8 已完成的证据。

---

## 3. 依赖与阻塞分析

### 3.1 P3 未做，是不是卡在 P2？—— 是，且是硬阻塞

按 `task-dependency.mermaid` 的边：`T7 --> T8`、`T6 -.delayEvent Handler.-> T8`、`T4 --> T9`、`T8 --> T9`。

实际阻塞链（自底向上）：

```
T5 (Profile)  ❌未开工
  ├─→ 无 SlotAmount   → T8 无法校验 slotNum 上界；T9 无法渲染 N 路开关
  ├─→ 无 Kind 持久化  → T7/T8 的品类校验在重启后 fail-open（AnShengCommandSpec.cs:54）
  └─→ 无 Version      → 固件门槛（q / uploadEnable）无法判定

T6 (事件管线) ❌未开工
  └─→ 无 delayEvent Handler → T8 延时任务镜像无法被事件更新（验收标准 4 不可达）
  └─→ 无 timeEvent Handler  → T10 定时任务镜像无法就地更新（验收标准 4 不可达）

T7 (命令服务) ❌未开工
  ├─→ 无 AnShengCommandRecord / 在途表 → T8/T10/T11 均无「下发-应答」闭环与超时观测
  └─→ 无 GET /catalog                  → T4 的 Catalog 驱动控制台无数据源
  └─→ 无 GET /commands/{commandId}     → T9 验收标准 2「按 commandId 轮询」无端点

⇒ T8/T9（P3）在 T5/T6/T7 落地前，即使强行开工也无法通过自身验收标准。
```

**结论**：P3 不是「忘了做」，而是**其三个前置任务（P2 全部）从未启动**。  
7-31 日志「依联调结果决定 Phase 2」的决策点**至今未关闭** —— 联调（4G 开关 IMEI 863434084755211）已于 7-31~8-03 完成并打通全链路，  
但**未见据此启动 P2 的记录**。这是当前进度的**真实卡点**。

### 3.2 ⚠️ T4 前端清理欠账的现实影响 —— 已经有坏掉的按钮（最高优先级发现）

7-31 日志的原始约定是：

> 「前端清理（T4）刻意延后，待 T8 `action`/`actions` 就绪后与之同批切换，**避免前端出现「伪命令已删、真命令未到」的空窗期**」

**但代码实证表明：这个空窗期不仅没有被避免，而且已经实际发生了 —— 因为后端在 T3 已物理删除，而 T8 至今未开工。**

**证据链**：

**① 前端仍有 4 个伪命令按钮**（用户可见、可点击）：

```tsx
// Web/src/app/pages/AnShengManagementPage.tsx:119-161
const OPEN_DEVICE_COMMAND_TEMPLATES: CommandTemplate[] = [
  { method: 'setSwitch',       label: '控制开关',     ... },   // :121
  { method: 'getSwitchStatus', label: '查询开关状态', ... },   // :132
  { method: 'setSwitchConfig', label: '配置开关',     ... },   // :142
  { method: 'getSwitchConfig', label: '查询开关配置', ... },   // :153
  { method: 'reboot',          label: '重启设备',     ... },   // :163  ← 此项正常
  ...
];
// :1020  {OPEN_DEVICE_COMMAND_TEMPLATES.map(tpl => (   ← 渲染为按钮
```

**② 前端 API 仍指向已被物理删除的后端端点**：

```ts
// Web/src/app/services/api/anshengApi.ts
:72-79   controlSwitch    → POST /ansheng/{id}/switch          ← 后端已删除
:82-90   getSwitchStatus  → GET  /ansheng/{id}/switch-status   ← 后端已删除
:93-100  configureSwitch  → POST /ansheng/{id}/switch-config   ← 后端已删除
```

对照 `AnShengController.cs` 实际端点（`:49/:68/:145/:274/:349/:388`）—— 这 3 条路由**均不存在**。  
用户决策为「直接物理删除，不保留 410 端点」，因此调用结果是 **404**，而非设计文档 T3 验收标准 2 描述的「410 + 指引新端点」。

**③ 三个按钮的失败路径**（`AnShengManagementPage.tsx:427-440`）：

```tsx
if (isOpenDevice && method === 'setSwitch') {
  response = await anshengApi.controlSwitch({...})       // :430 → 404
} else if (isOpenDevice && method === 'getSwitchStatus') {
  response = await anshengApi.getSwitchStatus(...)       // :433 → 404
} else if (isOpenDevice && method === 'setSwitchConfig') {
  response = await anshengApi.configureSwitch({...})     // :438 → 404
}
```

**④ 第 4 个按钮 `getSwitchConfig` 的问题更严重 —— 它不是 404，而是会向真实设备下发一条协议中不存在的报文**：

该 method 未被上面的 if-else 捕获，落入 `:442` 的通用分支：

```tsx
} else {
  // 通用命令（getDevInfo、getSwitchConfig、getDevStatus 等）      // :442
  response = await anshengApi.sendCommand({ method: selectedTemplate.method, ... });   // :450
}
```

→ 命中后端 `POST /api/v1/ansheng/{id}/command`（`AnShengController.cs:349`）  
→ `AnShengCommandService.SendCommandAsync`：`AnShengCommandCatalog.TryGet("getSwitchConfig", ...)` **返回 false**（T1 已确保 Catalog 无伪命令）  
→ **`:104` 的整个校验块被跳过**（校验只在 `TryGet` 成功时执行）  
→ 进入适配器 `AnShengMqttProtocolAdapter.cs:350`：`Catalog.Contains("getSwitchConfig")` 为 false  
→ **落入 `:355` 的 Legacy 兜底分支**，用 `BuildLegacyCommand` 构造 `param` 包裹的充电桩格式报文  
→ **`:373-377` 真实 publish 到设备 topic**

**净效果**：点击「查询开关配置」会向现网 4G 开关设备发送一条 `{"method":"getSwitchConfig","param":{...}}` 的**协议外报文**。  
设备大概率忽略（不会损坏），但这是**静默的错误行为**：前端显示"已下发"，实际是污染流量、无应答、无告警。

**⑤ 附带的 T4 遗留问题**（设计文档 T4 验收标准 3 明确要求清零）：

```tsx
// Web/src/app/pages/AnShengManagementPage.tsx
:982   d.model.includes('Speaker') || d.model.includes('Switch')
:1005  d.model.includes('Speaker') || d.model.includes('Switch')
```

→ 字符串型号判定仍在使用，与 T5 的品类模型完全脱节。

**影响评级**：

| 问题                             |     严重度    | 说明                          |
| ------------------------------ | :--------: | --------------------------- |
| 3 个按钮 404                      |    **中**   | 用户可见的功能性故障，但失败可见、无副作用       |
| `getSwitchConfig` 向设备发协议外报文    |    **高**   | 静默错误 + 污染现网设备流量 + 前端误报成功    |
| Catalog 外 method 兜底走 Legacy 下发 | **高（架构性）** | 任何拼写错误都会被真实下发而非快速失败，是上一条的根因 |
| `model.includes('Speaker')` 残留 |      低     | 技术债，T4/T9 一并清理              |

**建议**：不要等 T8 一起做。**T4 的「删除部分」应立即单独执行**（成本极低，见 §4），  
把 4 个伪命令模板 + 3 个死 API 摘掉，先止血；Catalog 驱动的控制台部分再随 T7 落地。  
同时建议给 Catalog 外 method 的 Legacy 兜底加白名单（仅 `orderStart`/`orderEnd`/`orderUp`/`close` 等已知充电桩命令放行，其余快速失败）。

### 3.3 两套 Phase 编号造成的误读风险

核查中确认仓库内**同时存在两套 Phase 命名**，极易误判进度：

| 文件                                              | 属于哪套编号                         | 实际含义                                                                   |
| ----------------------------------------------- | ------------------------------ | ---------------------------------------------------------------------- |
| `docs/phase2-smoke-test.ps1.md`（7-31 15:59）     | **旧套**（安圣 MQTT 设备接入 Phase 1–4） | 内容为「创建协议配置 → 连 broker 120.79.3.248:18883 → 认领设备」的**联调冒烟脚本**，与 T5–T7 无关 |
| `.workbuddy/design/ansheng-open-redesign.md` §6 | **新套**（T1–T14 / P1–P5）         | 本次核查依据                                                                 |
| 工作日志中的 T01/T02                                  | 均非上述两套                         | 7-31 之后真机联调修复的内部编号                                                     |

> ⚠️ **`docs/phase2-smoke-test.ps1.md` 这个文件名具有强误导性** —— 它会让人误以为「Phase 2 已做到冒烟测试阶段」。  
> 建议重命名为 `docs/ansheng-fieldtest-smoke-test.md` 或加注释头声明其编号体系。

---

## 4. 剩余工作量估算

> 口径：以设计文档标注的文件数为基准，按本次核查的实际代码状态调整。  
> 「新增」= 需从零创建；「修改」= 在既有文件上改动。人日为粗估，含自测但不含真机联调。

| 任务                  |  设计文件数 |          实际待办（新增/修改）          | 涉及模块                                           |      粗估人日     | 备注                                                                   |
| ------------------- | :----: | :---------------------------: | ---------------------------------------------- | :-----------: | -------------------------------------------------------------------- |
| **T4-删除部分**（止血）     |    —   |             0 / 3             | Web                                            |    **0.3**    | 摘 4 模板 + 3 死 API + if-else 分支；**建议立即做**                              |
| **T5** Profile      |    6   |      3 / 3 + 1 Migration      | Models, Services, Data                         |       3       | 品类三级回退 + 认领强制探测；R2 已关闭（2026-08-03 采用 netType 推断）                     |
| **T6** 事件管线         |   12   |      11 / 3 + 1 Migration     | Infrastructure, Services, Data                 |       5       | 7 个 Handler + Router + Normalizer + Dispatcher；含 close 30s 去抖        |
| **T7** 命令服务重构       |    7   |      3 / 4 + 1 Migration      | Services, Controllers, Data                    |       4       | 在途表 TTL + 命令记录 + 3 个新端点；R14 已定单实例，只做内存实现                             |
| **P2 小计**           |   25   | **17 新 / 10 改 + 3 Migration** |                                                |     **12**    |                                                                      |
| **T8** 开关+延时（后端）    |    5   |      3 / 2 + 1 Migration      | Controllers, Models, Services, DTOs            |       3       | 依赖 T5/T6/T7                                                          |
| **T9** 开关面板（前端）     |    4   |             2 / 2             | Web（需新建 `features/ansheng/`）                   |       3       | 目录结构从零搭                                                              |
| **P3 小计**           |    9   |  **5 新 / 4 改 + 1 Migration**  |                                                |     **6**     |                                                                      |
| **T10** 定时任务        |    7   |      3 / 3 + 1 Migration      | Models, Services, Controllers, Web, DTOs       |       4       | R3 已关闭（2026-08-03），整表版+单插槽版全做；补 slotNum 越界校验验收                       |
| **T11** 电量计         |    7   |      4 / 3 + 1 Migration      | Models, Services, Controllers, Web, DTOs       |       5       | 聚合表幂等 + 48 项 hourSumData + 4 个校准命令；R8 已关闭（2026-08-03），仍依赖 SlotAmount |
| **P4 小计**           |   14   |  **7 新 / 6 改 + 2 Migration**  |                                                |     **9**     |                                                                      |
| **T12** 运维配置        |    8   |      4 / 4 + 1 Migration      | Controllers, Models, Infrastructure, DTOs, Web |       5       | 9 个 method + 多组 topic；R4/R11 仍待 T12 确认（2026-08-03 未答复）               |
| **T13** MQTT 参数     |    4   |             1 / 3             | Controllers, DTOs, Services, Web               |       2       | 高危，重点在校验与审计而非功能量                                                     |
| **T14** 协议族隔离       |    4   |             1 / 3             | Infrastructure, Services, Web                  |    **1.5**    | **已完成约 50%**，剩余为文件归位 + `ProtocolFamily` 字段 + 前端分区                    |
| **P5 小计**           |   16   |  **6 新 / 10 改 + 1 Migration** |                                                |    **8.5**    |                                                                      |
| **测试脚手架**（新增项）      |    —   |              2 新              | tests                                          |     **2**     | `WebApplicationFactory` + 内存 DbContext；**T8 之后的验收标准依赖它**             |
| **合计（P2+P3+P4+P5）** | **64** | **37 新 / 30 改 / 7 Migration** |                                                | **≈ 37.5 人日** | 不含真机联调与协议歧义澄清往返                                                      |

**模块影响面汇总**：

| 模块                                 |    待新增文件    | 说明                                                                                                                                                     |
| ---------------------------------- | :---------: | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `Models/`                          |      9      | Profile, DeviceEvent, CommandRecord, DelayTask, TimeTask, EmStatistic, KeyConfig, SimCheckConfig, Rs485Record                                          |
| `Services/`                        |      13     | ProfileService, EventDispatcher, 7×EventHandler, PendingCommandStore, ScheduleService, EnergyService + 接口若干                                            |
| `Controllers/`                     |      4      | Switch, Schedule, Energy, Maintenance                                                                                                                  |
| `Infrastructure/Protocol/AnSheng/` |      3      | MessageRouter, DataNormalizer, Legacy/LegacyCommandBuilder                                                                                             |
| `Web/src/app/features/ansheng/`    |      8      | 目录从零创建：CommandConsole, SwitchControlPanel, DeviceEventTimeline, ScheduleEditor, EnergyStatisticsPanel, MqttParamsEditor, useAnShengCatalog, deviceKind |
| `Data/`                            | 7 Migration | T5/T6/T7/T8/T10/T11/T12 各一                                                                                                                             |
| `tests/`                           |      2      | 集成测试脚手架                                                                                                                                                |

---

## 5. 建议的下一步执行顺序

### 第 0 步（立即，独立于一切）：T4 止血 —— 0.3 人日

仅做「删除」部分，不等 T8：

1. 删 `AnShengManagementPage.tsx:119-161` 中 4 个伪命令模板（保留 `reboot`/`getDevInfo` 等真命令）
2. 删 `anshengApi.ts:72-100` 三个死 API 及 `ansheng.types.ts` 对应类型
3. 删 `AnShengManagementPage.tsx:427-438` 三个 if-else 分支
4. **加固**：给 `AnShengMqttProtocolAdapter.cs:355` 的 Legacy 兜底分支加白名单，非已知充电桩 method 直接抛错，避免协议外报文外发
5. 验证 `npm run build` 无 TS 错误

> 理由：这是当前**唯一已经影响线上可用性**的问题，成本极低，且不依赖任何未完成任务。  
> 原「与 T8 同批切换」的约定已失效 —— 因为 T8 至少还有 12 人日的前置依赖，空窗期只会继续拉长。

### 第 1 步：关闭协议歧义（并行，不占开发工时）— ✅ 已完成（2026-08-03）

向安圣确认，避免 T10/T11/T12 返工：

- **R3**（阻塞 T10）：`getSlotTimeTasks`/`setSlotTimeTasks` 是否有 `slotNum` 参数 → ✅ **已关闭**：安圣确认含 `slotNum`（`setSlotTimeTasks` 必填）
- **R8**（影响 T11）：`getEMStatistics.data[]` 与插槽的对应关系 → ✅ **已关闭**：按插槽 1~n 顺序（SlotNum=index+1）
- **R2**（影响 T5）：型号 ↔ 品类对照表 → ✅ **已关闭**：经 `getDevStatus.netType` 推断（值域 {4G,WiFi}），不再等对照表
- **R9**（影响 T10）：`getTimeTasks.tasks[]` 与插槽的对应关系 → ✅ **已关闭（2026-08-03，同构推定）**：与 R8 的 `data[]` 同构，按插槽 1~n 顺序（SlotNum=index+1），并继承 `Profile.SlotAmount` 长度校验护栏。⚠ 属推定非厂商逐条确认，T10 联调需复核
- **R4 / R11**（影响 T12）：`simCheck` 事件字段、beta 命令稳定性 → ⏳ **仍待 T12 确认**（用户未答复）

### 第 2 步：补测试脚手架 —— 2 人日

`WebApplicationFactory` + 内存/容器化 DbContext。  
**先于 T5 落地**，否则 T5 之后所有任务的集成型验收标准都无法自动化验证（见 §2.11）。

### 第 3 步：P2 全量 —— 12 人日（严格按 T5 → T6 → T7 顺序）

- **T5 优先**：它是 T7 校验可靠性（fail-open 问题）、T8 slotNum 上界、T9 N 路渲染、T11 数组长度校验的**共同前置**
- **T6 次之**：`delayEvent`/`timeEvent` Handler 是 T8/T10 镜像更新的前提
- **T7 收口**：完成后同步为 T4 补齐 Catalog 驱动控制台（`GET /catalog` 就绪）
- 出口检查：T5–T7 共 16 条验收标准全绿，且 §2.11 的 3 项 `KnownGap` 中第 2 项（slotNum 上界）随 T5 关闭

### 第 4 步：P3 —— 6 人日

T8（后端）→ T9（前端）。完成后即达成设计文档里程碑「二开开关设备核心功能可用」。  
**这是用户价值拐点** —— 在此之前，二开开关设备在平台上实际不可控。

### 第 5 步：P4 —— 9 人日

T10 与 T11 **可并行**（依赖不同：T10←T8，T11←T7）。  
（R3 已于 2026-08-03 关闭，`setSlotTimeTasks` 含 `slotNum`，T10 整表版 + 单插槽版均需完整实现。）

### 第 6 步：P5 —— 8.5 人日 

- **1T14 优先**（仅 1.5 人日且已完成一半），先把协议族隔离显式化，消除 §3.2 ④ 的架构性隐患
- T12 次之，T13 最后（高危，建议独立评审 + 测试设备先行验证）

### 关键路径总览

```
T4止血(0.3) ─┐
             ├─→ 测试脚手架(2) → T5(3) → T6(5) → T7(4) → T8(3) → T9(3) → T10(4)‖T11(5) → T14(1.5) → T12(5) → T13(2)
协议歧义确认 ─┘
                                        └─ P2: 12 ─┘   └ P3: 6 ┘   └─ P4: 9 ─┘   └───── P5: 8.5 ─────┘

关键路径 ≈ 37.5 人日（P4 内部并行可压缩约 4 人日 → ≈ 33.5 人日）
```

---

## 6. 核查方法与可复核性说明

**本次核查执行的检索命令**（均可复现）：

```bash
# 目录级存在性
ls Infrastructure/Protocol/AnSheng/ Infrastructure/Protocol/AnSheng/Legacy/ \
   Services/AnShengEventHandlers/ Web/src/app/features/ansheng/

# 三级状态区分（以 T8 为例）
grep -rnE "startDelayTask|stopDelayTask|getDelayTasks|delayEvent" Infrastructure --include=*.cs   # 第1/2级
grep -rnE "startDelayTask|stopDelayTask|getDelayTasks|delayEvent" \
     Services Controllers DTOs Models Data --include=*.cs                                          # 第3级
grep -rnE "startDelayTask|stopDelayTask|getDelayTasks|delayEvent" Web/src                          # 前端

# 端点清单
grep -nE "\[Http(Get|Post|Put|Delete)|\[Route" Controllers/AnShengController.cs

# 迁移与 DbSet
ls Migrations/ ; grep -nE "DbSet<AnSheng|DbSet<Discovered" Data/AppDbContext.cs

# 测试覆盖边界
grep -rnE "AnShengCommandService|Controller|WebApplicationFactory|DbContext" tests/IoTPlatform.AnSheng.Tests/*.cs

# 编译验证
dotnet build IoTPlatform.sln -v q --nologo    # → 0 错误 / 81 警告
```

**未做的事**（受核查纪律约束）：

- 未修改任何生产代码；本次唯一写入的文件是本报告
- 未运行 `dotnet test`（测试工程仅覆盖协议层，对本次「任务是否交付」的判定无增量信息）
- 未通读 `asopen.md`（5320 行协议原文，非本次核查对象）

**判定置信度**：

| 判定                                  |  置信度  | 说明                             |
| ----------------------------------- | :---: | ------------------------------ |
| T5/T6/T7/T9/T10/T11/T12/T13 = ❌ 未开工 | **高** | 交付物文件不存在 + 全文检索零命中，双重证据        |
| T8 = ❌ 未开工                          | **高** | Builder 有方法但业务层命中全为注释，已逐条列出    |
| T14 = 🟡 部分完成                       | **高** | 已完成/未完成两侧均有行号级证据               |
| T4 造成坏按钮                            | **高** | 前端调用点、后端端点清单、Legacy 兜底路径三处证据闭环 |
| 工作量估算                               | **中** | 基于设计文档文件数 + 实际状态调整，未做详细任务拆解    |

---

**报告结束**

> 核查人：QA 工程师 严过关  
> 生成时间：2026-08-03  
> 报告路径：`H:\IoTPlatform\docs\phase345-audit-2026-08-03.md`
